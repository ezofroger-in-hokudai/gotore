# task.md

## タスク一覧

### タスク名: 仕様・実装の監査と改善バックログの整備
- 目的: 仕様との不整合、継続利用を妨げる不具合、表示・保存の高速化、未採用の機能案を追跡可能なIssueへ整理する。
- 主な実装内容: 関連資料・既存Issue・コード・テストの照合、モックブラウザでの再現、端末保存量・通信回数の計測、根拠と受け入れ条件を付けたIssue登録。
- 完了条件: 重複を除いたIssueに確認基準・再現結果または提案であること・優先度案・依存関係を記載し、監査記録へリンクする。
- 備考: 2026-09-10のユーザー依頼。監査・16件のIssue登録（#69〜#84）を完了し、docs/issue-review-2026-09-10.mdへ記録した。仕様採用・実装修正・担当割当・今週の計画変更は各Issueで別途決める。既存の未コミット変更・未追跡資料を保持する。

### タスク名: ライブ状態の強調とプロフィール画像（#67）
- 目的: いま一緒に記録している仲間を見つけやすくし、設定から自分の画像を変更できるようにする。
- 主な実装内容: 右下の赤丸・LIVE表示、新着セットの短い強調と相対時刻、期限・通信断の反映、画像のプレビュー・保存・削除と本人/同一グループ限定の取得。
- 完了条件: 画像の入力制約・認可・永続化と、LIVEの期限・失敗・再取得を検証し、モバイル表示・既存の記録共有が通る。
- 備考: 実装・検証完了（backend140件・frontend単体29件・実Supabaseを含むE2E57件、lint/build・DB lint）。ユーザー追加依頼を週次計画#33の使用後改善として扱う。仕様はdocs/live-presence-avatars.md。既存のLIVE判定・共有規則を維持する。本番migrationと実機での写真選択は未実施。

### タスク名: 最高記録の視覚表示と接続待ちの削減（PR #64追加）
- 目的: 一覧でも最高記録を見つけやすくし、説明と縦幅を減らし、一般的な高速化手法を比較・適用する。
- 主な実装内容: 今回のトレーニングのセット別強調、BESTの表示文言削減、比較RMの横配置、次種目を左・次セットを右へ変更、DB接続プールの導入と計測。
- 完了条件: 保存確定・再起動・訂正後も最高記録表示が整合し、接続の再利用・上限・エラー復旧と本人限定の参照を検証する。make check・実Supabase E2E・比較結果をPR #64へ反映する。
- 備考: 実装・検証完了（backend128件・frontend単体29件・実Supabaseを含むE2E52件、lint/build）。ユーザーの追加依頼。#64は未マージなので同じブランチで対応。接続プールは既存Psycopgの公式拡張を使い、リージョン・認証・DB schemaは変更しない。

### タスク名: 記録操作の応答と最高記録の強調（PR #63追加フィードバック）
- 目的: セット追加後の状態を明確にし、開始・終了の待ちと操作の迷いを減らす。
- 主な実装内容: タッチ後に残るホバー色の修正、セット番号付き受付・同期表示、種目追加の末尾配置、次種目・終了のボタン化、BESTの赤色・炎表示、セッションAPIのDB往復削減。
- 完了条件: 通信遅延・失敗・再送時も保存状態を判別でき、共有・revision・終了規則を維持する。実DBの性能比較、make check、実Supabase E2E、モバイル画面を検証しPRを作る。
- 備考: 実装・検証完了（backend120件・frontend単体29件・実Supabaseを含むE2E50件、lint/build）。ユーザーの使用後フィードバックによる週次計画#33の追加修正。PR #63はマージ済みのため、最新main aba5bc3からfix/training-feedback-latencyで対応。新規基盤・migrationなし。画像と比較結果を添えてPRへ提出する。

### タスク名: 読み込みと複数種目の記録導線（#61・#62）
- 目的: 読み込みによる待ち・画面の揺れと、次の種目や独自種目を追加しにくい問題を解消する。
- 主な実装内容: 画面内キャッシュと再確認、読み込み領域の確保、記録ホームの全種目・セット一覧、一覧スクロール中も見える入力・追加操作、種目登録の直接導線。
- 完了条件: 遅延・失敗時も表示状態を区別でき、複数種目と独自種目を同じトレーニングへ記録できる。全セットを確認でき、追加操作が画面外へ押し出されない。make check・E2Eで検証する。
- 備考: ローカル実装・検証完了（backend118件・frontend単体29件・E2E46件、lint/build）。追加migrationなし。ユーザーの#61・#62指定による週次計画#33への追加作業。両Issueは未担当・依存指定なし、PR #60はマージ済み。共通の記録画面に関わるためfix/61-62-training-experienceで対応。レビュー担当は未定。

### タスク名: 記録画面の連続入力とバックグラウンド保存
- 目的: スクロール・ホイールの遅さ・通信待ちでセット入力を止めない。
- 主な実装内容: コンパクトな記録画面、連続ホイール、常時表示のメモ、端末に永続化する送信待ちキューと順序付き保存・復元・競合保護。
- 完了条件: 通信を保留しても次セットを入力でき、再起動・再送でも二重追加や消失がない。通常のモバイル画面で入力・保存・メモ・終了をスクロールせず操作できる。
- 備考: メモの本文表示・前回メモの条件付き表示・ボタン短縮も実装・検証済み。PR #60でレビュー。v2確認後のユーザー指摘に基づく追加作業。PR #59はマージ済みのためfix/session-input-flowで対応。ローカル実装・検証完了（backend118件・frontend単体29件・E2E41件、lint/build）。ホームのデザインは維持。追加migrationなし。実機の操作感は別途確認する。

### タスク名: GOTORE v2の画面・遷移の整合性確認と実装
- 目的: 確定版v2を現行アプリへ接続したときの共有・記録・集計の矛盾を防ぐ。
- 主な実装内容: 同梱資料・Figma全33画面の遷移と現行仕様・コード・テストを照合し、採用判断後に専用ブランチで画面・必要なAPI/DB・回帰テストを実装する。
- 完了条件: 未決定の業務ルールを合意し、既存データと非共有設定を保持してv2の受け入れ条件を検証する。
- 備考: ユーザー回答で共有・終了/復元・LIVE・12桁招待・必要スクロール・通知後続を確定。feat/gotore-v2でローカル実装・検証完了（backend118件・frontend単体21件・E2E35件、lint/build、DB lint成功）。分析はdocs/gotore-v2-review.md、採用規則はdocs/gotore-v2-spec.md。開始時の公開中PRは0件。#8・#30の担当変更なし。通知Issue #21へ後続条件を追記済み。実機確認と本番反映は未実施。

### タスク名: 記録画面の情報と硬派な画面デザイン（#56）
- 目的: 入力中の種目の実績と注意点を見ながら記録できるようにする。
- 主な実装内容: 推定1RM・最高重量・種目メモを表示し、通常操作のスクロールを避ける構成と、控えめな文字・直線主体の共通ルールへ更新。
- 完了条件: スマホ幅で見本を確認し、#8・#17・#22へ画面要件と実装上の残作業を引き継ぐ。
- 備考: 文書化はimtkgtr。画面適用は#8（IXYZONE）、BEST機能は#17、種目メモは#22。

### タスク名: スマホ向けデザインルールと画面テンプレート（#54）
- 目的: 将来のスマホアプリと現在のWebで、一貫した見やすさと操作感を作る。
- 主な実装内容: モバイル操作・短い文言・赤の使い方・文字と余白・共通部品・画面見本を文書化。
- 完了条件: 合意方針と実装基準をdocsへ記録し、#8・#30から参照できる。
- 備考: 文書化はimtkgtr。画面実装の担当はIXYZONEのまま。ネイティブ基盤の選定は別途。

### タスク名: 読み込み速度の計測・改善（#11）
- 目的: 毎回の画面読み込みの待ち時間を減らす。
- 主な実装内容: API各段階の計測、接続再利用・プロフィールSQL・リージョン配置の比較。
- 完了条件: 改善前後と採用理由をIssueへ記録し、認証・共有範囲・再試行を維持して検証する。本番の認証済み画面は別途計測する。
- 備考: imtkgtr担当。週次計画#33への追加。5秒更新と非表示停止は維持。

### タスク名: 残る画面文言の短縮（#50）
- 目的: ログイン・エラー・通知・ガイドの文字量をさらに減らす。
- 主な実装内容: 状態と次の操作だけを短く表示し、通常画面の補足・二重通知を除く。
- 完了条件: 入力制約と共有・復帰操作を維持し、既存テストとCIで検証してmainへ反映する。
- 備考: #49後のユーザー追加依頼。種目の共有モデルは変更しない。

### タスク名: 画面文言の簡略化とPR統合（#48）
- 目的: 通常画面の文字数を減らし、確認済み機能を一緒に利用できるようにする。
- 主な実装内容: 全画面の文言整理、初回ガイド更新、PR競合解消、統合テスト。
- 完了条件: 各機能を保持したmake check・共有E2Eが成功し、CIを確認してmainへマージする。
- 備考: ユーザーのレビュー・マージ依頼に基づく週次計画#33への追加。種目の共有モデルは完了後に相談する。


### タスク名: 開発手順・環境変数・migration運用の整備

- 目的: 新しい参加者が初回起動から日々の更新・DB変更・検証・PRまで進められるようにする。
- 主な実装内容: READMEの手順整理、環境変数サンプルの用途明記、ローカルmigration適用・履歴確認コマンド、共有DBへの反映手順。
- 完了条件: 実装とCLIに合うコマンド・環境変数・反映タイミングが記載され、通常適用とデータを削除するresetを区別できる。文書リンク・コマンド・サンプルを検証し、push・PRで共有する。
- 備考: ユーザーからの追加依頼。週次計画 #33 の追加作業として記録し、既存機能PRとは独立してmainを起点に整備。手順・コマンド・サンプルのローカル確認、push・PR #37 の作成完了。CIの実DB確認と第三者レビューはPRで追跡する。クラウド設定・DB変更・デプロイは行わない。

### タスク名: 初回利用ガイド（#18）

- 目的: 初回ログイン後に参加・記録・共有範囲を理解して通常操作へ進める。
- 主な実装内容: 3段階の案内、スキップ、ユーザー別の表示済み保存、設定からの再表示。
- 完了条件: 案内が通常操作や下書きを妨げず、表示状態・再表示・保存失敗と共有フローのテストが通る。画像付きPRで共有する。
- 備考: 未担当Issueをimtkgtrへ割り当てた後に着手。仕様はdocs/onboarding.md。実装・make check・UIテストと画像確認済み。DB/API変更は不要。実Supabase E2EはPRのCIで確認する。

### タスク名: 招待コード再発行（#20）

- 目的: オーナーが共有済みの招待コードを無効にし、新しいコードを発行できるようにする。
- 主な実装内容: オーナー限定API、期待コードによる競合検知、参加との行ロック、確認・再取得・コピーUI。
- 完了条件: 旧コード拒否と新コード参加、所属・記録保持、権限・競合・失敗を検証し、画像付きPRで共有する。
- 備考: 未担当#20をimtkgtrへ割り当て後に着手。仕様はdocs/invite-code.md。招待停止状態・期限は今回導入しない。

### タスク名: 本人の記録編集・削除（#6）

- 目的: 保存後の入力間違いを本人が訂正・削除し、共有先にも整合した内容を表示する。
- 主な実装内容: revision付き編集・削除API、削除済みID保持、本人用の確認・編集画面、新規下書きの保護。
- 完了条件: 所有権・入力制約・共有反映・競合・再送をDB/API/画面テストで確認し、画像付きPRへ共有する。
- 備考: 未担当Issueをimtkgtrへ割り当て後に着手。仕様はdocs/workout-management.md。migration先行適用が必要。


### タスク名: 表示名がアクセス時に既定値へ戻る問題の調査・修正（#29）

- 目的: 表示名の保存元と同期経路を確認し、意図しない名前の消失を防ぐ。
- 主な実装内容: Auth未設定時のプロフィール同期と設定画面の参照元を調査し、合意した保持ルールに沿って回帰テスト・実装・文書を更新する。
- 完了条件: 合意した再現手順で既存名が消えず、設定画面と共有記録の表示が一致する。通常の名称変更と共有権限を維持する。
- 備考: Authに有効名があれば優先し、未設定ならDBの既存名を保持する方針をユーザー承認済み。先行テストで不具合を検出し、同期・設定画面・仕様を修正。make check（backend61件・frontend11件）とブラウザ11件成功。Docker停止で実Supabase依存2件は未完了。レビュー・push・PR・本番反映は別途。失われた名前の自動復元は行わない。

### タスク名: 認証設定エラーの再発防止（#24）

- 目的: 不正な認証環境変数による未処理500を防ぎ、安全に原因を確認できるようにする。
- 主な実装内容: リクエスト時のURL・キー検証、秘密値を含まない診断、回帰テスト、公開手順の補足。
- 完了条件: 不正設定は503、不正トークンは401となり、正常な認証とhealthを維持する。設定値を応答・ログへ出さない。
- 備考: 実装とmake check完了、コミットf24aa99。クラウド設定・デプロイ・リモートCIは未実施。

### タスク名: 日常操作の改善（#4・#5・#7）

- 目的: 入力の手間を減らし、利用者とグループの表示名を変更できるようにする。
- 主な実装内容: 重量から回数へのEnter移動・最後の回数から次セット追加、設定画面の表示名変更、オーナーによるグループ名変更。
- 完了条件: 入力制約・下書き・共有権限を維持して操作でき、API・画面の回帰テストが通る。
- 備考: 2026-09-07のユーザー承認範囲を実装。make check（一時PostgreSQL）とUI単独ブラウザ検証済み。Docker停止により実Supabase共有E2Eは未完了。メール・パスワード変更、メンバー削除、記録削除は未採用のまま残す。レビュー担当と週次振り返りはチームで決める。

### タスク名: テスト運用の管理者登録・ログイン専用化

- 目的: 確認メールの送信制限に依存せず、管理者が用意した利用者だけで記録共有を試す。
- 主な実装内容: 新規登録UIの削除、Supabaseの一般登録禁止、管理者による確認済みアカウント作成手順、ログイン・共有の回帰テスト。
- 完了条件: 新規登録を案内せず、管理者作成アカウントでログイン・共有・再ログインできる。クラウドで必要な手動設定と未実施範囲が明確である。
- 備考: アプリとローカル設定・文書の変更、make checkとE2E5件の検証完了。公開Supabaseの設定や実アカウント作成は管理者が行う。公開反映と自己登録の再開は別途対応する。

### タスク名: Vercel Servicesによるmonorepo公開準備

- 目的: Next.jsとFastAPIを1つのVercelプロジェクト・ドメインへ配置できる設定を用意する。
- 主な実装内容: Services設定、API経路とローカル転送の分離、Supabase接続プールへの対応、環境変数・公開手順、回帰テスト。
- 完了条件: Servicesの設定を検査でき、本番向けbuildと既存のローカル共有フローが通る。公開時に必要な設定と未検証範囲が明記される。
- 備考: ユーザーが確認後に追加を依頼した方式（Services Beta）を採用し、設定とローカル検証は完了。今回はリポジトリ内の準備のみで、クラウドプロジェクト作成・DB migration適用・デプロイは未実施。

### タスク名: チーム開発の初期環境・運用整備

- 目的: 現行システムを起点に、必要な機能を小さく実装し、毎週改善できる開発環境を整える。
- 主な実装内容: 参加者向けREADME、開発・レビュー手順、エージェント向け規約、Issue・PR・週次計画テンプレート、仕様と現状の案内、共通の検証コマンドを整備する。
- 完了条件: 新しい参加者が起動・検証方法を確認でき、現状に基づくIssueからPRまでの進め方と、Android／iOSへの移植を見据えた責務分離が明文化されている。
- 備考: ローカルの環境・運用整備は完了。GitHub側の保護設定と初回週次計画はチームで行う。アプリ実装はスタンダード版v0.1のタスクで進める。モバイル技術の選定、デプロイは別タスク。

### タスク名: スタンダード版v0.1の仕様確定と実装

- 目的: 開発メンバーと仲間が日々のトレーニングを記録・共有して継続利用できる初版を作る。
- 主な実装内容: 実装範囲を合意し、API・DB・権限の仕様とIssueを整備して、機能単位で実装・テストする。機能候補は `docs/standard-v0.1-scope.md` に記載する。
- 完了条件: 合意した受け入れ条件を満たし、複数ユーザーで記録・共有と過去記録の保持を確認できる。ホーム画面への追加に必要な設定、テストと起動手順が揃う。
- 備考: ユーザー指定の「グループ作成・記録・記録共有」のローカル実装・自動検証が完了。本人を識別するログインとDBへの継続保存を含む。追加指定により、初版はWebページをホーム画面に追加して使うオンラインのWebアプリとする。HTTPS公開と実機での追加・再起動確認は未実施。スタンプ・SCORE・ランキング等は後続。

### タスク名: セット数の活動ヒートマップと日別記録（#35・親#14）

- 目的: 本人のトレーニング活動を月別に振り返り、日付から記録を確認できるようにする。
- 主な実装内容: 月の日別セット数集計API、カレンダー型ヒートマップ、日付を選んだ記録一覧、本人限定・月境界・50件超・失敗復帰のテスト。
- 完了条件: 実記録全件から日別のセット数を合算し、色と数値で表示する。日付タップでその日の全記録へページングでき、他人の記録は含めない。
- 備考: ユーザーがまずセット数でのヒートマップと日付タップを指定。SCOREは#13、BEST・継続日数・種目別推移は#14の後続範囲。週次計画#33の#14枠で実装。make check成功（backend63件・frontend単体13件）、UI12件成功。Docker停止で実Supabase依存2件はPRのCIで補完する。

### タスク名: 本人用の種目リストと選択入力（#28）

- 目的: 毎回自由入力せず、自分の種目リストから選んで記録できるようにする。
- 主な実装内容: 本人専用の種目一覧・追加・削除APIとDB、選択入力、管理UI、認可・履歴保持・下書きの回帰テスト。
- 完了条件: 初回8種目を選べ、追加・削除が再ログイン後も保持される。他人の候補に触れず、削除後も過去記録と下書きを保持し、記録共有が通る。
- 備考: 2026-09-07の会話で本人用候補と自由入力から選択への変更を確認。複数種目をまとめるメニュープリセットは#17、分析の採用条件は#14で別途扱う。実装後のmake check成功（backend56件・frontend11件）、UI検証12件相当成功。Docker停止で実Supabase依存2件はCIで確認する。

### タスク名: グループの所属管理
- 目的: 不要な参加を解消し、本人の履歴を残す。
- 主な実装内容: オーナーによるメンバー除外、自主退出、共有解除と再参加・再送の整合性。
- 完了条件: 権限別と共有履歴の保持をAPI・DB・画面で検証し、画像付きPRを作成する。
- 備考: #7・#19をimtkgtrへ割り当ててから着手。所有者移譲・#31の削除には踏み込まない。

### タスク名: 本人履歴からの記録入力
- 目的: 同じメニューを毎回入力する負担を減らす。
- 主な実装内容: 本人履歴の再利用、新しいID・日付・非共有の下書き、置き換え確認。
- 完了条件: 元の記録とキャンセル時の下書きを保護し、単体・E2E・画像付きPRで確認する。
- 備考: #17から#41を追加しimtkgtrへ割り当ててから実装する。

### タスク名: 本人のトレーニング振り返りメモ
- 目的: 数値だけで表せない気づきを本人だけに残す。
- 主な実装内容: 非公開メモの取得・保存・空欄化、競合・再送、削除連動とUI。
- 完了条件: 他人や共有APIへ漏れず、失敗時の入力保持と境界値を検証し画像付きPRを作る。
- 備考: #22から#43を分割し、imtkgtrへ割り当ててから着手。

### タスク名: iOS移植の比較調査（#9）
- 目的: 現行Webを起点に、方式を選ぶための変更範囲・費用・実機検証条件を整理する。
- 主な実装内容: Web継続・ラッパー・React Native・ネイティブの比較、API・認証・下書きの境界、公式資料と検証案。
- 完了条件: 推奨する検証順・未決事項・Androidとの共通部分を資料に残し、文書PRを作成する。
- 備考: 未担当#9をimtkgtrへ割り当ててから調査。新SDK導入・方式採用・課金・配布は行わない。

### タスク名: 未担当Issueの実装と週次整理
- 目的: 仕様から実装できる改善を進め、追加機能と残る前提を追跡する。
- 主な実装内容: 担当設定後の機能別実装・テスト・画像付きPR、必要な子Issueの追加、実装済みIssueと保留条件の整理。
- 完了条件: 判断可能な対象をPRまで共有し、対象外・保留の理由と検証状態を週次計画とdocsへ残す。
- 備考: #33で管理。今回マージ・本番操作は実行しない。各PRの詳細はdocs/issue-review-2026-09-08.mdを参照。

### タスク名: 監査で確認した記録・画面復帰の不具合修正
- 目的: 意図しないセット保存と画面復帰時の不整合を解消し、非表示画面の不要な再取得を減らす。
- 主な実装内容: #69のEnter操作、#71の対象グループ保持、#72の履歴月保持、#73の初回通信失敗からの復帰、#79の比較・BEST取得制御。先行回帰テストと全体検証。
- 完了条件: 採用した操作を仕様へ記録し、関連Issueの受け入れ条件をテスト・PRで確認できる。
- 備考: ユーザーの実装依頼で週次計画#33に追加。共通画面の回帰確認をまとめるため1ブランチとし、目的ごとにコミットする。#71・#72・#73・#79は実装・標準検証済み。#69の重量Enterは修正済み、回数Enterは追加回答により保存操作を採用（下記タスク）。SCORE等の未確定ルールは別Issue。

### タスク名: 一覧取得の復旧・グループ表示の通信削減と型検査
- 目的: 読み込み待ちから画面内で復旧できるようにし、不要なフィード取得と型検査の抜けを減らす。
- 主な実装内容: #74の読み取り待機期限、#78の所属グループ概要取得と選択フィード取得、#86のBun型定義・共通型検査。先行テストと比較計測。
- 完了条件: 待機期限後の再試行・遅延応答の排除・共有権限とLIVE鮮度の維持、1/5/10グループ比較、make checkと実Supabase E2Eを確認してPR化する。
- 備考: 継続依頼により週次計画#33へ追加。#69の通常回数Enterは保存操作として追加タスクで対応。

### タスク名: 記録の増減操作と友達の共有記録詳細
- 目的: 記録中の数値調整を分かりやすくし、ホームから友達のトレーニング内容を確認できるようにする。
- 主な実装内容: #69の回数Enterを保存操作へ統一、#88の重量±1kg・回数±1回の矢印、#89の共有記録詳細。
- 完了条件: 入力制約・二重保存防止・共有権限を維持し、ブラウザとDBで検証してPR化する。
- 備考: 2026-09-11のユーザー追加依頼。#89はタップした記録1件を表示する回答を受け、下記の詳細閲覧タスクで実装。

### タスク名: 共有フィードから1件の記録詳細を閲覧
- 目的: ホームで気になった友達のセットから、トレーニング全体を確認できるようにする。
- 主な実装内容: #89の所属・共有先を確認する単一記録API、全種目・全セットの閲覧シート、開閉・再取得・失敗の制御。
- 完了条件: 非共有・別グループ・退会後・削除後の閲覧とメモ漏えいを防ぎ、DB・ブラウザ・実Supabaseで検証し画像付きPRにする。
- 備考: ユーザーは「タップした記録」を指定。同日の別記録は含めない。週次計画#33、担当imtkgtr。mainからIssue別ブランチで実装し、PR #90統合後の状態も検証する。

### タスク名: 共有記録詳細の戻り先と読み込みの改善
- 目的: 詳細を閉じても仲間の記録を表示し、閲覧時の待ちを減らす。
- 主な実装内容: #92の選択グループ・履歴保持の回帰修正、#93の詳細先読み・再取得の整理と計測。
- 完了条件: 開閉・戻る・共有解除・失敗を検証し、取得回数と表示待ちを比較して画像付きPRへ提出する。
- 備考: ユーザーが実際に利用して追加したIssueを週次計画#33へ追加。#93は表示中の仲間の詳細を先読みし、LIVEを優先更新する回答を受けた。閲覧・記録・履歴の待ちも削減する。共通の共有画面を修正するため同じブランチで目的別にコミットする。

### タスク名: 開始待ちの入力と種目比較の先読み
- 目的: 開始通信や種目選択のたびに記録操作が止まる時間を減らす。
- 主な実装内容: #95の開始前入力、開始直後の通常入力画面への遷移と比較の並行読み込み、候補種目の前回比較・BEST・メモの上限付き先読み。
- 完了条件: 開始確定前の保存防止・失敗再試行・復元・無効化を検証し、標準チェックと実Supabase E2Eを通して画像付きPRにする。
- 備考: 実使用後のユーザー追加依頼で週次計画#33に追加。API計算量は#80、端末保存のコピー削減は#77で追跡。

### タスク名: 履歴グラフ・グループ集計の高速な閲覧
- 目的: 自分と仲間の推移を、待ち時間の少ない切り替えで確認する。
- 主な実装内容: 個人4指標・週月集計、グループグラフ/ランキング、DB集計、先読み/キャッシュ。
- 完了条件: 合意した集計・共有範囲・更新整合性を満たし、単体/DB/E2E検証と性能確認をPRに記載する。
- 備考: #97、関連 #14 #15。SCORE算式は #13。仕様は docs/history-analytics.md。

### タスク名: SCORE・個人目標・終了後のコメントと共有表示
- 目的: 自分の実績への到達度と目標に沿う振り返りを、終了後と仲間の画面で待ち時間を抑えて確認する。
- 主な実装内容: 採点ドメインと根拠保存、開始時の目標固定、設定からの目標変更、終了結果画面、LLM評価と一言、共有済み記録のスコア表示。
- 完了条件: 採用仕様を記録し、算式・目標バージョン・公開範囲・訂正削除・失敗時・重複実行防止を検証、make checkと記録共有E2E・表示性能の結果を画像付きPRへ掲載する。
- 備考: #99、関連 #13 #16、週次計画 #33。docs/score.md の仮案とAI提案を本人が確認する目標設定を採用。安さ・速さを優先する。標準目標と訂正時の単独再採点は承認済み。API・画面・再利用を実装し、画像付きPR #101を作成。全体make check成功、ローカルE2E未完了分はPRのCIで確認。実モデルの速度・判定品質はキー設定後に #100 で確認。
