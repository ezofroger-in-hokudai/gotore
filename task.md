# task.md

## タスク名: 記録画面の採用デザインを実装
- 目的: 記録の入力・編集・メモ・ライブ通知を、合意した短く分かりやすい操作へ統一する。
- 主な実装内容: 重量0.5kg刻み、入力ホイール開閉、保存済みセットの同一欄編集、メモ入口の一本化、通常同期表示の削減、ライブスタンプ表示と回数制限の見直し。
- 完了条件: 320/390/430pxで入力・編集・メモ・終了が操作でき、端末下書き・保存競合・共有範囲を維持し、関連テストが成功する。
- 備考: #186を設計根拠とする。デザイン確定までPRの作成・更新はしない。

## タスク一覧

### タスク名: 記録中の入力・メモ保護と直近履歴の統合
- 目的: 保存中の次入力を失わず、記録操作と終了後の振り返りを短くする。
- 主な実装内容: PR #115の入力保持・種目復帰・今回セットのスクロール・メモ復元・操作領域を最新mainへ統合し、PR #117の直近3件を現在の部位カレンダー・全セット表へ適合させる。
- 完了条件: 記録・復元・共有、操作領域と文字拡大、直近履歴の展開・絞り込み・詳細復帰を検証して各PRへ反映する。
- 備考: ユーザーが①〜⑤を承認。⑥の分析整理（#113/PR #119）は今回保留。週次#149の追加指定として、既存PRの履歴を保持して進める。

### タスク名: 開始前の種目管理と一覧への即時反映（#148）
- 目的: 整理のためにトレーニングを開始する手間と、追加/編集後の表示待ちを減らす。
- 主な実装内容: 設定の入口、管理一覧の検索/部位選択、追加フォームの開閉、行内の削除確認、候補データの画面間共用と成功応答の反映。
- 完了条件: 開始/実績保存なしの管理、追加通信なしの絞り込み、失敗/競合/再取得の保持、3幅・暗色・長い名前と記録共有を検証し、画像付きPRを提出する。
- 備考: 週次#149。API/DB、過去の名前、共有範囲は維持。名前変更・一括分類・並べ替えは別の候補。

### タスク名: 次の実装候補と種目整理の棚卸し
- 目的: 不要な機能を増やさず、現状の不便と未反映の修正を把握して優先順位を相談する。
- 主な実装内容: 棚卸し時は実装せず、main・Issue・未マージPRを照合し、記録・速度・振り返り・共有・グループ・種目整理の候補と採用判断が必要な点をまとめる。
- 完了条件: 新規候補/統合待ち/保留を区別した一覧をユーザーへ提示し、採用範囲を選べるようにする。
- 備考: 2026-09-14の追加指示で実装前の棚卸しを優先。#148は棚卸し後に継続実装を承認。週次#149に着手順を記録。docs/implementation-review-2026-09-14.mdを参照。

### タスク名: 筋トレキャラクターの読み込み表示（#146）
- 目的: 初回読み込みの空白を和らげ、合意済みの重複文言を整理する。
- 主な実装内容: 起動時だけのSVG/CSSアニメーション、採用したA案の丸いウォンバットと同じキャラのアイコン、動き低減とキャッシュ/失敗の表示分離。
- 完了条件: 読み込み完了を待たせず表示し、既存データを隠さない。3幅・ダーク・動き低減・再試行を確認し、画像付きPRへまとめる。
- 備考: ユーザーの2026-09-14追加依頼。各画面から起動時だけに変更。ユーザーがA案を選択し、起動キャラクターとアイコンを統一済み。標準チェック・関連E2E5件と実画像を確認し、PR #147でレビューする。文言は#140とPR #144で合意済みの範囲。#143の公開DB更新はユーザーが適用済みと回答。

### タスク名: 日別ヒートマップの部位表示と横一列フィルター
- 目的: 日付から鍛えた部位を見つけ、部位を切り替えて履歴をすぐ確認できるようにする。
- 主な実装内容: 現在の主部位での日別集計、固定8分類の部位選択、日付セルの部位名と内訳、既存月キャッシュの共用。
- 完了条件: 本人限定・再分類/削除・二重計上なし・0kg/未記録と、スマホの表示・選択・通信回数・保存共有を検証し画像付きPRを提出する。
- 備考: PR #139で固定8分類への変更と検証を実施中（#138）。#136の画面比較へのユーザー追加指定。「現在の分類で過去も表示し直す」は回答で採用済み。

### タスク名: 選択状態と重複する見出しの整理
- 目的: 現在の画面や選択済みタブから分かる文言を減らし、数値や操作を見つけやすくする。
- 主な実装内容: カレンダーの装飾見出し、種目選択の重複部位名、分析の重複した指標/ランキング名を削除。意味のある区切り・単位・状態と読み上げ名は保持。
- 完了条件: 主要画面を棚卸しし、必要な識別情報を保ったまま重複を整理し、既存チェックと関連画面で確認する。
- 備考: 2026-09-13の追加依頼。部位ヒートマップの画面修正版は#136/PR #137で扱う。

### タスク名: 最高重量・最高RMの記録表示を統一
- 目的: 履歴・記録中・グループ共有で、種目ごとの最高記録を炎と赤字で見つけやすくする。
- 主な実装内容: 保存済みセットの重量/RM別の最高記録判定、履歴・共有DTOへの位置情報、比較欄・詳細・フィードの表示、既存集計を使った一括取得。
- 完了条件: 初回/同値/訂正/取消/未送信・権限・保存共有を検証し、スマホ画像付きPRを提出する。
- 備考: 2026-09-13追加依頼、#134。実装・検証完了、画像付きPR #135へ提出。CIでbackend217件・frontend単体58件・E2E124件成功。部位選択PR #133はマージ済み。初回・同値を含めない既存の最高記録判定を維持する。

### タスク名: 種目の部位保存・表示・選択
- 目的: 種目と鍛える部位の関係を保存し、目的の部位から種目を選べるようにする。
- 主な実装内容: 本人の種目リストへの部位情報追加、追加/編集UI、部位フィルターと記録中の表示。部位は一覧と一緒に取得して端末内で切り替える。
- 完了条件: 部位を保存・編集・再取得でき、部位と検索を組み合わせて選べる。本人限定・失敗復帰・過去記録/下書き保持と記録共有を検証し、画像付きPRを提出する。
- 備考: 2026-09-13のユーザー依頼（#132）。画面を含むA案の承認を受け実装・検証完了、画像付きPR #133はマージ済み。CIでbackend210件・frontend単体58件・E2E121件成功。種目分類を再利用できるデータとして持たせる。部位別グラフやランキングは今回の分類保存から独立した後続範囲。


### タスク名: スコアの一時停止と総負荷量表示
- 目的: 採点を外し、記録した重量×回数の積み重ねをすぐ確認できるようにする。
- 主な実装内容: 採点・目標設定・AI呼び出しの停止、日別総負荷ヒートマップ、終了画面の総負荷表示。
- 完了条件: 保存・共有・履歴の挙動を維持し、総負荷集計と採点停止をAPI・画面で検証する。
- 備考: ユーザーの2026-09-13依頼（#130）。実装とローカル検証を完了し、画像付きPR #131でレビュー。過去のスコア・目標データは削除しない。週次計画#33への使用後改善。色は月内相対を暫定採用。


### タスク名: 仕様・実装の監査と改善バックログの整備
- 目的: 仕様との不整合、継続利用を妨げる不具合、表示・保存の高速化、未採用の機能案を追跡可能なIssueへ整理する。
- 主な実装内容: 関連資料・既存Issue・コード・テストの照合、モックブラウザでの再現、端末保存量・通信回数の計測、根拠と受け入れ条件を付けたIssue登録。
- 完了条件: 重複を除いたIssueに確認基準・再現結果または提案であること・優先度案・依存関係を記載し、監査記録へリンクする。
- 備考: 2026-09-10のユーザー依頼。監査・16件のIssue登録（#69〜#84）を完了し、docs/issue-review-2026-09-10.mdへ記録した。仕様採用・実装修正・担当割当・今週の計画変更は各Issueで別途決める。既存の未コミット変更・未追跡資料を保持する。

### タスク名: ライブ状態の強調とプロフィール画像（#67）
- 目的: いま一緒に記録している仲間を見つけやすくし、設定から自分の画像を変更できるようにする。
- 主な実装内容: 右下の赤丸・LIVE表示、新着セットの短い強調と相対時刻、期限・通信断の反映、画像のプレビュー・保存・削除と本人/同一グループ限定の取得。
- 完了条件: 画像の入力制約・認可・永続化と、LIVEの期限・失敗・再取得を検証し、モバイル表示・既存の記録共有が通る。
- 備考: 実装・検証完了（backend140件・frontend単体29件・実Supabaseを含むE2E57件、lint/build・DB lint）。ユーザー追加依頼を週次計画#33の使用後改善として扱う。仕様はdocs/live-presence-avatars.md。既存のLIVE判定・共有規則を維持する。本番migrationと実機での写真選択は未実施。

### タスク名: 最高記録の視覚表示と接続待ちの削減（PR #64追加）
- 目的: 一覧でも最高記録を見つけやすくし、説明と縦幅を減らし、一般的な高速化手法を比較・適用する。
- 主な実装内容: 今回のトレーニングのセット別強調、BESTの表示文言削減、比較RMの横配置、次種目を左・次セットを右へ変更、DB接続プールの導入と計測。
- 完了条件: 保存確定・再起動・訂正後も最高記録表示が整合し、接続の再利用・上限・エラー復旧と本人限定の参照を検証する。make check・実Supabase E2E・比較結果をPR #64へ反映する。
- 備考: 実装・検証完了（backend128件・frontend単体29件・実Supabaseを含むE2E52件、lint/build）。ユーザーの追加依頼。#64は未マージなので同じブランチで対応。接続プールは既存Psycopgの公式拡張を使い、リージョン・認証・DB schemaは変更しない。

### タスク名: 記録操作の応答と最高記録の強調（PR #63追加フィードバック）
- 目的: セット追加後の状態を明確にし、開始・終了の待ちと操作の迷いを減らす。
- 主な実装内容: タッチ後に残るホバー色の修正、セット番号付き受付・同期表示、種目追加の末尾配置、次種目・終了のボタン化、BESTの赤色・炎表示、セッションAPIのDB往復削減。
- 完了条件: 通信遅延・失敗・再送時も保存状態を判別でき、共有・revision・終了規則を維持する。実DBの性能比較、make check、実Supabase E2E、モバイル画面を検証しPRを作る。
- 備考: 実装・検証完了（backend120件・frontend単体29件・実Supabaseを含むE2E50件、lint/build）。ユーザーの使用後フィードバックによる週次計画#33の追加修正。PR #63はマージ済みのため、最新main aba5bc3からfix/training-feedback-latencyで対応。新規基盤・migrationなし。画像と比較結果を添えてPRへ提出する。

### タスク名: 読み込みと複数種目の記録導線（#61・#62）
- 目的: 読み込みによる待ち・画面の揺れと、次の種目や独自種目を追加しにくい問題を解消する。
- 主な実装内容: 画面内キャッシュと再確認、読み込み領域の確保、記録ホームの全種目・セット一覧、一覧スクロール中も見える入力・追加操作、種目登録の直接導線。
- 完了条件: 遅延・失敗時も表示状態を区別でき、複数種目と独自種目を同じトレーニングへ記録できる。全セットを確認でき、追加操作が画面外へ押し出されない。make check・E2Eで検証する。
- 備考: ローカル実装・検証完了（backend118件・frontend単体29件・E2E46件、lint/build）。追加migrationなし。ユーザーの#61・#62指定による週次計画#33への追加作業。両Issueは未担当・依存指定なし、PR #60はマージ済み。共通の記録画面に関わるためfix/61-62-training-experienceで対応。レビュー担当は未定。

### タスク名: 記録画面の連続入力とバックグラウンド保存
- 目的: スクロール・ホイールの遅さ・通信待ちでセット入力を止めない。
- 主な実装内容: コンパクトな記録画面、連続ホイール、常時表示のメモ、端末に永続化する送信待ちキューと順序付き保存・復元・競合保護。
- 完了条件: 通信を保留しても次セットを入力でき、再起動・再送でも二重追加や消失がない。通常のモバイル画面で入力・保存・メモ・終了をスクロールせず操作できる。
- 備考: メモの本文表示・前回メモの条件付き表示・ボタン短縮も実装・検証済み。PR #60でレビュー。v2確認後のユーザー指摘に基づく追加作業。PR #59はマージ済みのためfix/session-input-flowで対応。ローカル実装・検証完了（backend118件・frontend単体29件・E2E41件、lint/build）。ホームのデザインは維持。追加migrationなし。実機の操作感は別途確認する。

### タスク名: GOTORE v2の画面・遷移の整合性確認と実装
- 目的: 確定版v2を現行アプリへ接続したときの共有・記録・集計の矛盾を防ぐ。
- 主な実装内容: 同梱資料・Figma全33画面の遷移と現行仕様・コード・テストを照合し、採用判断後に専用ブランチで画面・必要なAPI/DB・回帰テストを実装する。
- 完了条件: 未決定の業務ルールを合意し、既存データと非共有設定を保持してv2の受け入れ条件を検証する。
- 備考: ユーザー回答で共有・終了/復元・LIVE・12桁招待・必要スクロール・通知後続を確定。feat/gotore-v2でローカル実装・検証完了（backend118件・frontend単体21件・E2E35件、lint/build、DB lint成功）。分析はdocs/gotore-v2-review.md、採用規則はdocs/gotore-v2-spec.md。開始時の公開中PRは0件。#8・#30の担当変更なし。通知Issue #21へ後続条件を追記済み。実機確認と本番反映は未実施。

### タスク名: 記録画面の情報と硬派な画面デザイン（#56）
- 目的: 入力中の種目の実績と注意点を見ながら記録できるようにする。
- 主な実装内容: 推定1RM・最高重量・種目メモを表示し、通常操作のスクロールを避ける構成と、控えめな文字・直線主体の共通ルールへ更新。
- 完了条件: スマホ幅で見本を確認し、#8・#17・#22へ画面要件と実装上の残作業を引き継ぐ。
- 備考: 文書化はimtkgtr。画面適用は#8（IXYZONE）、BEST機能は#17、種目メモは#22。

### タスク名: スマホ向けデザインルールと画面テンプレート（#54）
- 目的: 将来のスマホアプリと現在のWebで、一貫した見やすさと操作感を作る。
- 主な実装内容: モバイル操作・短い文言・赤の使い方・文字と余白・共通部品・画面見本を文書化。
- 完了条件: 合意方針と実装基準をdocsへ記録し、#8・#30から参照できる。
- 備考: 文書化はimtkgtr。画面実装の担当はIXYZONEのまま。ネイティブ基盤の選定は別途。

### タスク名: 読み込み速度の計測・改善（#11）
- 目的: 毎回の画面読み込みの待ち時間を減らす。
- 主な実装内容: API各段階の計測、接続再利用・プロフィールSQL・リージョン配置の比較。
- 完了条件: 改善前後と採用理由をIssueへ記録し、認証・共有範囲・再試行を維持して検証する。本番の認証済み画面は別途計測する。
- 備考: imtkgtr担当。週次計画#33への追加。5秒更新と非表示停止は維持。

### タスク名: 残る画面文言の短縮（#50）
- 目的: ログイン・エラー・通知・ガイドの文字量をさらに減らす。
- 主な実装内容: 状態と次の操作だけを短く表示し、通常画面の補足・二重通知を除く。
- 完了条件: 入力制約と共有・復帰操作を維持し、既存テストとCIで検証してmainへ反映する。
- 備考: #49後のユーザー追加依頼。種目の共有モデルは変更しない。

### タスク名: 画面文言の簡略化とPR統合（#48）
- 目的: 通常画面の文字数を減らし、確認済み機能を一緒に利用できるようにする。
- 主な実装内容: 全画面の文言整理、初回ガイド更新、PR競合解消、統合テスト。
- 完了条件: 各機能を保持したmake check・共有E2Eが成功し、CIを確認してmainへマージする。
- 備考: ユーザーのレビュー・マージ依頼に基づく週次計画#33への追加。種目の共有モデルは完了後に相談する。


### タスク名: 開発手順・環境変数・migration運用の整備

- 目的: 新しい参加者が初回起動から日々の更新・DB変更・検証・PRまで進められるようにする。
- 主な実装内容: READMEの手順整理、環境変数サンプルの用途明記、ローカルmigration適用・履歴確認コマンド、共有DBへの反映手順。
- 完了条件: 実装とCLIに合うコマンド・環境変数・反映タイミングが記載され、通常適用とデータを削除するresetを区別できる。文書リンク・コマンド・サンプルを検証し、push・PRで共有する。
- 備考: ユーザーからの追加依頼。週次計画 #33 の追加作業として記録し、既存機能PRとは独立してmainを起点に整備。手順・コマンド・サンプルのローカル確認、push・PR #37 の作成完了。CIの実DB確認と第三者レビューはPRで追跡する。クラウド設定・DB変更・デプロイは行わない。

### タスク名: 初回利用ガイド（#18）

- 目的: 初回ログイン後に参加・記録・共有範囲を理解して通常操作へ進める。
- 主な実装内容: 3段階の案内、スキップ、ユーザー別の表示済み保存、設定からの再表示。
- 完了条件: 案内が通常操作や下書きを妨げず、表示状態・再表示・保存失敗と共有フローのテストが通る。画像付きPRで共有する。
- 備考: 未担当Issueをimtkgtrへ割り当てた後に着手。仕様はdocs/onboarding.md。実装・make check・UIテストと画像確認済み。DB/API変更は不要。実Supabase E2EはPRのCIで確認する。

### タスク名: 招待コード再発行（#20）

- 目的: オーナーが共有済みの招待コードを無効にし、新しいコードを発行できるようにする。
- 主な実装内容: オーナー限定API、期待コードによる競合検知、参加との行ロック、確認・再取得・コピーUI。
- 完了条件: 旧コード拒否と新コード参加、所属・記録保持、権限・競合・失敗を検証し、画像付きPRで共有する。
- 備考: 未担当#20をimtkgtrへ割り当て後に着手。仕様はdocs/invite-code.md。招待停止状態・期限は今回導入しない。

### タスク名: 本人の記録編集・削除（#6）

- 目的: 保存後の入力間違いを本人が訂正・削除し、共有先にも整合した内容を表示する。
- 主な実装内容: revision付き編集・削除API、削除済みID保持、本人用の確認・編集画面、新規下書きの保護。
- 完了条件: 所有権・入力制約・共有反映・競合・再送をDB/API/画面テストで確認し、画像付きPRへ共有する。
- 備考: 未担当Issueをimtkgtrへ割り当て後に着手。仕様はdocs/workout-management.md。migration先行適用が必要。


### タスク名: 表示名がアクセス時に既定値へ戻る問題の調査・修正（#29）

- 目的: 表示名の保存元と同期経路を確認し、意図しない名前の消失を防ぐ。
- 主な実装内容: Auth未設定時のプロフィール同期と設定画面の参照元を調査し、合意した保持ルールに沿って回帰テスト・実装・文書を更新する。
- 完了条件: 合意した再現手順で既存名が消えず、設定画面と共有記録の表示が一致する。通常の名称変更と共有権限を維持する。
- 備考: Authに有効名があれば優先し、未設定ならDBの既存名を保持する方針をユーザー承認済み。先行テストで不具合を検出し、同期・設定画面・仕様を修正。make check（backend61件・frontend11件）とブラウザ11件成功。Docker停止で実Supabase依存2件は未完了。レビュー・push・PR・本番反映は別途。失われた名前の自動復元は行わない。

### タスク名: 認証設定エラーの再発防止（#24）

- 目的: 不正な認証環境変数による未処理500を防ぎ、安全に原因を確認できるようにする。
- 主な実装内容: リクエスト時のURL・キー検証、秘密値を含まない診断、回帰テスト、公開手順の補足。
- 完了条件: 不正設定は503、不正トークンは401となり、正常な認証とhealthを維持する。設定値を応答・ログへ出さない。
- 備考: 実装とmake check完了、コミットf24aa99。クラウド設定・デプロイ・リモートCIは未実施。

### タスク名: 日常操作の改善（#4・#5・#7）

- 目的: 入力の手間を減らし、利用者とグループの表示名を変更できるようにする。
- 主な実装内容: 重量から回数へのEnter移動・最後の回数から次セット追加、設定画面の表示名変更、オーナーによるグループ名変更。
- 完了条件: 入力制約・下書き・共有権限を維持して操作でき、API・画面の回帰テストが通る。
- 備考: 2026-09-07のユーザー承認範囲を実装。make check（一時PostgreSQL）とUI単独ブラウザ検証済み。Docker停止により実Supabase共有E2Eは未完了。メール・パスワード変更、メンバー削除、記録削除は未採用のまま残す。レビュー担当と週次振り返りはチームで決める。

### タスク名: テスト運用の管理者登録・ログイン専用化

- 目的: 確認メールの送信制限に依存せず、管理者が用意した利用者だけで記録共有を試す。
- 主な実装内容: 新規登録UIの削除、Supabaseの一般登録禁止、管理者による確認済みアカウント作成手順、ログイン・共有の回帰テスト。
- 完了条件: 新規登録を案内せず、管理者作成アカウントでログイン・共有・再ログインできる。クラウドで必要な手動設定と未実施範囲が明確である。
- 備考: アプリとローカル設定・文書の変更、make checkとE2E5件の検証完了。公開Supabaseの設定や実アカウント作成は管理者が行う。公開反映と自己登録の再開は別途対応する。

### タスク名: Vercel Servicesによるmonorepo公開準備

- 目的: Next.jsとFastAPIを1つのVercelプロジェクト・ドメインへ配置できる設定を用意する。
- 主な実装内容: Services設定、API経路とローカル転送の分離、Supabase接続プールへの対応、環境変数・公開手順、回帰テスト。
- 完了条件: Servicesの設定を検査でき、本番向けbuildと既存のローカル共有フローが通る。公開時に必要な設定と未検証範囲が明記される。
- 備考: ユーザーが確認後に追加を依頼した方式（Services Beta）を採用し、設定とローカル検証は完了。今回はリポジトリ内の準備のみで、クラウドプロジェクト作成・DB migration適用・デプロイは未実施。

### タスク名: チーム開発の初期環境・運用整備

- 目的: 現行システムを起点に、必要な機能を小さく実装し、毎週改善できる開発環境を整える。
- 主な実装内容: 参加者向けREADME、開発・レビュー手順、エージェント向け規約、Issue・PR・週次計画テンプレート、仕様と現状の案内、共通の検証コマンドを整備する。
- 完了条件: 新しい参加者が起動・検証方法を確認でき、現状に基づくIssueからPRまでの進め方と、Android／iOSへの移植を見据えた責務分離が明文化されている。
- 備考: ローカルの環境・運用整備は完了。GitHub側の保護設定と初回週次計画はチームで行う。アプリ実装はスタンダード版v0.1のタスクで進める。モバイル技術の選定、デプロイは別タスク。

### タスク名: スタンダード版v0.1の仕様確定と実装

- 目的: 開発メンバーと仲間が日々のトレーニングを記録・共有して継続利用できる初版を作る。
- 主な実装内容: 実装範囲を合意し、API・DB・権限の仕様とIssueを整備して、機能単位で実装・テストする。機能候補は `docs/standard-v0.1-scope.md` に記載する。
- 完了条件: 合意した受け入れ条件を満たし、複数ユーザーで記録・共有と過去記録の保持を確認できる。ホーム画面への追加に必要な設定、テストと起動手順が揃う。
- 備考: ユーザー指定の「グループ作成・記録・記録共有」のローカル実装・自動検証が完了。本人を識別するログインとDBへの継続保存を含む。追加指定により、初版はWebページをホーム画面に追加して使うオンラインのWebアプリとする。HTTPS公開と実機での追加・再起動確認は未実施。スタンプ・SCORE・ランキング等は後続。

### タスク名: セット数の活動ヒートマップと日別記録（#35・親#14）

- 目的: 本人のトレーニング活動を月別に振り返り、日付から記録を確認できるようにする。
- 主な実装内容: 月の日別セット数集計API、カレンダー型ヒートマップ、日付を選んだ記録一覧、本人限定・月境界・50件超・失敗復帰のテスト。
- 完了条件: 実記録全件から日別のセット数を合算し、色と数値で表示する。日付タップでその日の全記録へページングでき、他人の記録は含めない。
- 備考: ユーザーがまずセット数でのヒートマップと日付タップを指定。SCOREは#13、BEST・継続日数・種目別推移は#14の後続範囲。週次計画#33の#14枠で実装。make check成功（backend63件・frontend単体13件）、UI12件成功。Docker停止で実Supabase依存2件はPRのCIで補完する。

### タスク名: 本人用の種目リストと選択入力（#28）

- 目的: 毎回自由入力せず、自分の種目リストから選んで記録できるようにする。
- 主な実装内容: 本人専用の種目一覧・追加・削除APIとDB、選択入力、管理UI、認可・履歴保持・下書きの回帰テスト。
- 完了条件: 初回8種目を選べ、追加・削除が再ログイン後も保持される。他人の候補に触れず、削除後も過去記録と下書きを保持し、記録共有が通る。
- 備考: 2026-09-07の会話で本人用候補と自由入力から選択への変更を確認。複数種目をまとめるメニュープリセットは#17、分析の採用条件は#14で別途扱う。実装後のmake check成功（backend56件・frontend11件）、UI検証12件相当成功。Docker停止で実Supabase依存2件はCIで確認する。

### タスク名: グループの所属管理
- 目的: 不要な参加を解消し、本人の履歴を残す。
- 主な実装内容: オーナーによるメンバー除外、自主退出、共有解除と再参加・再送の整合性。
- 完了条件: 権限別と共有履歴の保持をAPI・DB・画面で検証し、画像付きPRを作成する。
- 備考: #7・#19をimtkgtrへ割り当ててから着手。所有者移譲・#31の削除には踏み込まない。

### タスク名: 本人履歴からの記録入力
- 目的: 同じメニューを毎回入力する負担を減らす。
- 主な実装内容: 本人履歴の再利用、新しいID・日付・非共有の下書き、置き換え確認。
- 完了条件: 元の記録とキャンセル時の下書きを保護し、単体・E2E・画像付きPRで確認する。
- 備考: #17から#41を追加しimtkgtrへ割り当ててから実装する。

### タスク名: 本人のトレーニング振り返りメモ
- 目的: 数値だけで表せない気づきを本人だけに残す。
- 主な実装内容: 非公開メモの取得・保存・空欄化、競合・再送、削除連動とUI。
- 完了条件: 他人や共有APIへ漏れず、失敗時の入力保持と境界値を検証し画像付きPRを作る。
- 備考: #22から#43を分割し、imtkgtrへ割り当ててから着手。

### タスク名: iOS移植の比較調査（#9）
- 目的: 現行Webを起点に、方式を選ぶための変更範囲・費用・実機検証条件を整理する。
- 主な実装内容: Web継続・ラッパー・React Native・ネイティブの比較、API・認証・下書きの境界、公式資料と検証案。
- 完了条件: 推奨する検証順・未決事項・Androidとの共通部分を資料に残し、文書PRを作成する。
- 備考: 未担当#9をimtkgtrへ割り当ててから調査。新SDK導入・方式採用・課金・配布は行わない。

### タスク名: 未担当Issueの実装と週次整理
- 目的: 仕様から実装できる改善を進め、追加機能と残る前提を追跡する。
- 主な実装内容: 担当設定後の機能別実装・テスト・画像付きPR、必要な子Issueの追加、実装済みIssueと保留条件の整理。
- 完了条件: 判断可能な対象をPRまで共有し、対象外・保留の理由と検証状態を週次計画とdocsへ残す。
- 備考: #33で管理。今回マージ・本番操作は実行しない。各PRの詳細はdocs/issue-review-2026-09-08.mdを参照。

### タスク名: 監査で確認した記録・画面復帰の不具合修正
- 目的: 意図しないセット保存と画面復帰時の不整合を解消し、非表示画面の不要な再取得を減らす。
- 主な実装内容: #69のEnter操作、#71の対象グループ保持、#72の履歴月保持、#73の初回通信失敗からの復帰、#79の比較・BEST取得制御。先行回帰テストと全体検証。
- 完了条件: 採用した操作を仕様へ記録し、関連Issueの受け入れ条件をテスト・PRで確認できる。
- 備考: ユーザーの実装依頼で週次計画#33に追加。共通画面の回帰確認をまとめるため1ブランチとし、目的ごとにコミットする。#71・#72・#73・#79は実装・標準検証済み。#69の重量Enterは修正済み、回数Enterは追加回答により保存操作を採用（下記タスク）。SCORE等の未確定ルールは別Issue。

### タスク名: 一覧取得の復旧・グループ表示の通信削減と型検査
- 目的: 読み込み待ちから画面内で復旧できるようにし、不要なフィード取得と型検査の抜けを減らす。
- 主な実装内容: #74の読み取り待機期限、#78の所属グループ概要取得と選択フィード取得、#86のBun型定義・共通型検査。先行テストと比較計測。
- 完了条件: 待機期限後の再試行・遅延応答の排除・共有権限とLIVE鮮度の維持、1/5/10グループ比較、make checkと実Supabase E2Eを確認してPR化する。
- 備考: 継続依頼により週次計画#33へ追加。#69の通常回数Enterは保存操作として追加タスクで対応。

### タスク名: 記録の増減操作と友達の共有記録詳細
- 目的: 記録中の数値調整を分かりやすくし、ホームから友達のトレーニング内容を確認できるようにする。
- 主な実装内容: #69の回数Enterを保存操作へ統一、#88の重量±1kg・回数±1回の矢印、#89の共有記録詳細。
- 完了条件: 入力制約・二重保存防止・共有権限を維持し、ブラウザとDBで検証してPR化する。
- 備考: 2026-09-11のユーザー追加依頼。#89はタップした記録1件を表示する回答を受け、下記の詳細閲覧タスクで実装。

### タスク名: 共有フィードから1件の記録詳細を閲覧
- 目的: ホームで気になった友達のセットから、トレーニング全体を確認できるようにする。
- 主な実装内容: #89の所属・共有先を確認する単一記録API、全種目・全セットの閲覧シート、開閉・再取得・失敗の制御。
- 完了条件: 非共有・別グループ・退会後・削除後の閲覧とメモ漏えいを防ぎ、DB・ブラウザ・実Supabaseで検証し画像付きPRにする。
- 備考: ユーザーは「タップした記録」を指定。同日の別記録は含めない。週次計画#33、担当imtkgtr。mainからIssue別ブランチで実装し、PR #90統合後の状態も検証する。

### タスク名: 共有記録詳細の戻り先と読み込みの改善
- 目的: 詳細を閉じても仲間の記録を表示し、閲覧時の待ちを減らす。
- 主な実装内容: #92の選択グループ・履歴保持の回帰修正、#93の詳細先読み・再取得の整理と計測。
- 完了条件: 開閉・戻る・共有解除・失敗を検証し、取得回数と表示待ちを比較して画像付きPRへ提出する。
- 備考: ユーザーが実際に利用して追加したIssueを週次計画#33へ追加。#93は表示中の仲間の詳細を先読みし、LIVEを優先更新する回答を受けた。閲覧・記録・履歴の待ちも削減する。共通の共有画面を修正するため同じブランチで目的別にコミットする。

### タスク名: 開始待ちの入力と種目比較の先読み
- 目的: 開始通信や種目選択のたびに記録操作が止まる時間を減らす。
- 主な実装内容: #95の開始前入力、開始直後の通常入力画面への遷移と比較の並行読み込み、候補種目の前回比較・BEST・メモの上限付き先読み。
- 完了条件: 開始確定前の保存防止・失敗再試行・復元・無効化を検証し、標準チェックと実Supabase E2Eを通して画像付きPRにする。
- 備考: 実使用後のユーザー追加依頼で週次計画#33に追加。API計算量は#80、端末保存のコピー削減は#77で追跡。

### タスク名: 履歴グラフ・グループ集計の高速な閲覧
- 目的: 自分と仲間の推移を、待ち時間の少ない切り替えで確認する。
- 主な実装内容: 個人4指標・週月集計、グループグラフ/ランキング、DB集計、先読み/キャッシュ。
- 完了条件: 合意した集計・共有範囲・更新整合性を満たし、単体/DB/E2E検証と性能確認をPRに記載する。
- 備考: #97、関連 #14 #15。SCORE算式は #13。仕様は docs/history-analytics.md。

### タスク名: SCORE・個人目標・終了後のコメントと共有表示
- 目的: 自分の実績への到達度と目標に沿う振り返りを、終了後と仲間の画面で待ち時間を抑えて確認する。
- 主な実装内容: 採点ドメインと根拠保存、開始時の目標固定、設定からの目標変更、終了結果画面、LLM評価と一言、共有済み記録のスコア表示。
- 完了条件: 採用仕様を記録し、算式・目標バージョン・公開範囲・訂正削除・失敗時・重複実行防止を検証、make checkと記録共有E2E・表示性能の結果を画像付きPRへ掲載する。
- 備考: #99、関連 #13 #16、週次計画 #33。docs/score.md の仮案とAI提案を本人が確認する目標設定を採用。安さ・速さを優先し、追加指定でGPT-5 nanoと最小推論へ変更する。標準目標と訂正時の単独再採点は承認済み。API・画面・再利用を実装し、画像付きPR #101を作成。全体make check成功、ローカルE2E未完了分はPRのCIで確認。実モデルの速度・判定品質はキー設定後に #100 で確認。

### タスク名: SCOREの強調表示と日別スコアヒートマップ
- 目的: 仲間と自分の得点を一目で読み、終了後の達成感と日々の積み重ねを同じ配色で確認する。
- 主な実装内容: ホーム右側の大きな得点、終了結果の強調、目標条件の期間選択を簡略化、日別最高スコアのヒートマップと保存済み得点からの高速集計。
- 完了条件: 未評価と0点を区別し、同日複数回・訂正/削除・本人限定・色と得点の整合を検証。モバイル画像とmake check/E2EをPRへ反映する。
- 備考: PR #101へのユーザー追加依頼。日別集計は最高点を採用するとの回答済み。配色は高得点が赤・低得点が青、紫を避けた連続グラデーション、未実施は灰色との追加指定。


### タスク名: 開始前の振り返り表示と初回SCORE
- 目的: 開始前の入力画面の混在をなくし、直近の取り組みを確認してすぐ記録を始め、初回から得点を確認できるようにする。
- 主な実装内容: #102の開始後に種目選択する導線・直近記録と推移の先読み、#103の合意した初回基準と本人の過去比較への移行。
- 完了条件: 開始待ち・保存防止・復元・先読み/失敗と初回得点の境界を検証し、画像付きPRへまとめる。
- 備考: PR #101確認後のユーザー追加依頼、週次計画 #33。初回は各種目30回を100点、比較できないC/Iは100点、Gは通常AI評価で承認済み。みんなの記録の一言は #104へ記録のみとし、実装しない。


### タスク名: 記録ホイールの回転方向の統一
- 目的: 上下の増減ボタンと、指で値を中央へ回す感覚を揃える。
- 主な実装内容: スワイプを下で増加・上で減少へ反転し、移動中・確定時・慣性の方向を統一する。
- 完了条件: 重量/回数の方向・刻み・保存しないことと既存ボタン/直接入力を検証しPRへ反映する。
- 備考: ユーザー追加依頼 #108、週次計画 #33。Web入力のみ。

### タスク名: 追加Issueの継続実装
- 目的: ユーザーが再指定した範囲で、閲覧・記録・履歴確認を速く使いやすくする。
- 主な実装内容: 記録画面、ホーム/グループ導線、履歴/分析、通信/保存性能、合意後の新機能と仕様整理。
- 完了条件: docs/issue-implementation-plan.mdの全対象を受け入れ条件ごとに確認し、実装可能な部分を検証・PR化する。未回答や実機/外部条件が必要な項目は理由を明示して保持する。
- 備考: ユーザーが指定した#111/#107・高速化・画面整理・記録補助・グループ機能を対象とする。未回答の質問は後回しにし、旧計画のその他の機能は再開しない。

### タスク名: ホーム開始とグループ中心のナビゲーション
- 目的: 主要画面からすぐ記録を始め、下部ナビからグループへ直接アクセスできるようにする。
- 主な実装内容: #107の主要画面のフローティングSTART/RESUME（ホーム上部の重複ボタンは削除）、記録タブからグループタブへの変更、開始前の振り返りのホームへの移設。
- 完了条件: 既存セッション再開・開始待ち・種目選択・保存/終了・ブラウザ履歴・グループ選択、Sheet/キーボードと幅別表示を検証し画像付きPRにする。
- 備考: #102の開始前振り返りを維持する。実機PWAの確認は#23と区別し、API/DB変更はしない。assigneeに関係なく進める追加承認済み。

### タスク名: BESTと前回比較の履歴読み込みを削減
- 目的: 履歴が増えてもセット保存と仲間の記録閲覧の負担を抑える。
- 主な実装内容: #80の履歴10/100/1000件の計測、既存の種目別集計の利用、前回1件だけの取得、BEST更新者のまとめ取得。
- 完了条件: 前回順序・同名種目・RM丸め・訂正削除・共有範囲を維持し、SQL数/取得量/時間と回帰を検証してPRへ記載する。
- 備考: ユーザーが高速化の継続を指定。新規DB基盤は導入せず、既存集計の採用可否を実測する。

### タスク名: 更新失敗時の表示済みデータ保持
- 目的: 一時的な通信失敗で閲覧中の履歴・グループ・集計を消さず、再試行できるようにする。
- 主な実装内容: #111の一時失敗と権限/削除エラーの分類、同一取得先の保持、更新失敗表示と再試行、キャッシュの失効制御。
- 完了条件: 履歴・カレンダー・グループ・分析、初回失敗・別URL・権限喪失・非表示・再試行を検証しPRへまとめる。
- 備考: 全Issueの継続実装依頼。#112/#113とは別のmain起点で実装し、後で画面変更を統合しても状態処理を保持する。

### タスク名: 前回の最初のセットを入力初期値へ反映
- 目的: 毎回20kgから調整する手間を減らす。
- 主な実装内容: #120の前回最初の重量・回数を利用する初期値、先読みの共用、手入力・今回実績・復元入力の優先。
- 完了条件: 小数・0kg・前回なし・遅延・種目切替・再起動・保存しないことを検証しPRへまとめる。
- 備考: ユーザーの継続対象確認と同時の追加要望。API/DB変更なし。

### タスク名: 主要画面の説明を整理
- 目的: 数値・現在の状態・主操作を見つけやすくする。
- 主な実装内容: #114の文言棚卸し、装飾説明・未提供の通知項目の削除、指標説明の折りたたみ。
- 完了条件: 共有・未送信・エラー・削除確認を保持し、3幅の表示と既存の設定・分析操作を確認してPRへまとめる。
- 備考: ユーザーが画面整理の継続を指定。API・DB・業務ルールは変更しない。

### タスク名: 送信待ちの重複スナップショットを削減
- 目的: 長い送信待ちでも端末保存量と入力処理の負担を抑える。
- 主な実装内容: #77の確定基準＋順序付き差分の端末保存、既存v1読取、API送信時の各操作の復元、容量・破損・再送の検証。
- 完了条件: 30/150/600セットの保存量・処理時間、編集/取消、ACK待ち/応答喪失/複数タブ/旧形式を検証してPRへまとめる。
- 備考: ユーザーの高速化継続指定。API・DBは変えず、送信操作を間引かない。実機・送信量削減は別の条件として追跡。


### タスク名: グループの表示順を端末に保存
- 目的: よく見るグループを手前へ並べ、切替を短くする。
- 主な実装内容: #118のカードを長押ししたまま左右に移動する並べ替え、一覧シートの補助操作、本人別の端末保存。追加修正として通常スワイプの跳ね戻りと連続操作の取りこぼしを解消する。
- 完了条件: 長押し/通常タップ/スクロール、再読み込み、所属増減、保存失敗、ユーザー分離、選択維持を検証し画像付きPRにする。
- 備考: 2026-09-13の追加指定でこの端末への保存を採用。API/DBと共有範囲は変更しない。週次計画 #33への追加作業。

### タスク名: カレンダーの部位複数選択と画面文言の削減
- 目的: 胸と肩などをまとめて即座に振り返り、重複文言を減らして実績を読みやすくする。
- 主な実装内容: 取得済み月データによる部位の複数選択、重複しない記録件数、凡例・重複見出し・装飾文の整理。
- 完了条件: 複数選択・解除・すべて・0kg・旧応答・件数の重複防止・追加通信なしを検証し、文言整理の表示と保存共有を確認して画像付きPRにする。
- 備考: PR #139を前提とする追加依頼。文言提案A01〜A15/B01〜B09を採用。条件整理B10〜B12と比較候補Cは後続#141。ローカル検証完了、PR #139へ反映しCIとレビューを確認する。


### タスク名: 高速化PRの競合解消と統合
- 目的: 保留中の取得量・通信量・更新頻度改善を最新の画面と両立させてmainへ統合する。
- 主な実装内容: PR #124/#128/#121/#126の競合解消、現行のスコア停止・最高記録表示・部位複数選択の維持、統合検証。
- 完了条件: 各PRのコミットを保ち、標準チェックとE2E・CIを確認した統合用PRをマージする。
- 備考: ユーザーから競合解消とマージの明示依頼。画面構成変更の#115/#117/#119と試作#137は対象外。


### タスク名: セット保存の送信量を削減
- 目的: 保存待ちの再送で全状態を送る通信量を減らす。
- 主な実装内容: #77の保存JSONのgzip圧縮、上限付きのAPI展開、旧画面/旧API/非対応ブラウザとの互換、前後比較。
- 完了条件: 保存順・revision・再送・認可を保持し、圧縮/通常の保存・破損/過大本文・互換性を検証してPRへ提出する。
- 備考: 高速化継続指定。端末の差分保存PR #127とは独立し、APIの業務データ形式・DBを変更しない。


### タスク名: 情報の鮮度に応じた更新頻度の削減
- 目的: LIVEの反映速度を維持し、閲覧中の不要な通信を減らす。
- 主な実装内容: #11のLIVE/最新記録/所属/集計の更新頻度分離、画面復帰・保存後更新、取得済み分析の再利用。
- 完了条件: 1分の要求数、LIVE切替、非表示、遅延、保存・共有後の更新を検証しPRへまとめる。
- 備考: ユーザーの高速化継続指定。#111の表示保持PRを土台にする。本番認証済み計測と実機PWAは未検証として追跡。


### タスク名: 履歴取得エラーの調査と記録閲覧の画面比較
- 目的: 履歴の取得失敗を切り分け、本人・仲間の記録を迷わず読み取れる画面をユーザーと選ぶ。
- 主な実装内容: エラー発生経路・環境差の確認、既存の重複や情報密度の調査、3種類の操作可能な画面試作、本人履歴の端末キャッシュ案。
- 完了条件: 調査済み事実と未確認事項を区別し、履歴・共有・通信状態を比較できる案と確認結果をPR/Issueへ記録する。採用する画面はユーザー選択後に実装。
- 備考: 2026-09-13の追加依頼。現時点で本番URL/失敗したAPI・DBログは未確認。端末永続キャッシュは検討案で、共有の保持条件を未合意のまま変更しない。


### タスク名: A案の全セット表を本人・共有詳細へ実装
- 目的: 記録の重量・回数・推定1RMをひと目で読み取り、重複する情報を減らす。
- 主な実装内容: 全セットの表、総負荷・セット数・時間の要約、本人と共有のヘッダー整理、最高記録と既存操作の維持。
- 完了条件: 本人/共有、0kg・RM対象外・旧記録・長い種目名・画面幅と文字拡大、編集/コピー/メモ・再試行/権限喪失を検証して画像付きPRへ反映する。
- 備考: ユーザーが比較PR #144のA案を選択。端末永続保存は未採用、503は#143で調査を継続。

### タスク名: 実画面の初回機能案内（#151）
- 目的: 初めて使う人が、実際の操作場所と各機能の使い道を理解できるようにする。
- 主な実装内容: START・種目管理・カレンダー・グラフ・グループ・使い方の実画面へ赤枠/矢印/短い案内を重ね、閲覧画面を順に切り替える。
- 完了条件: 3幅/明暗/文字拡大/スクロール、キーボード・スキップ・再表示・既読分離・保存失敗・書込副作用なしを検証して画像付きPRにする。
- 備考: 週次#149への追加依頼。#104と独立。既読キーを維持し、既読ユーザーへ強制表示しない。

### タスク名: 共有記録の全体要約（#104）
- 目的: ホームで最新1セットだけでなく、取り組んだ種目数・セット数を把握できるようにする。
- 主な実装内容: 共有済み記録の集計を既存フィード応答に添え、詳細取得を待たずに短く表示。
- 完了条件: LIVE/終了/訂正/削除と共有解除、追加DB要求なし、モバイル表示を検証し画像付きPRにする。
- 備考: 週次#149。定型の事実要約を採用済み。部位・私的メモ・目標・AIは含めない。


### タスク名: Googleでの新規登録・ログイン（#156、関連 #25）
- 目的: 確認メールの配送を用意せず、基本情報だけのGoogle認証で利用を開始する。
- 主な実装内容: Googleログイン、認証からの復帰・失敗案内、初回表示名設定、メール新規登録を拒否するAuth hook、egotore.com向け設定手順。
- 完了条件: 基本権限だけを要求し、既存メールログイン・記録・表示名を保持する。キャンセル/接続失敗/再試行、登録制限、初回設定、共有を検証し、本番で残る設定を明記する。
- 備考: 2026-09-15のユーザー指定で週次#149へ追加する作業。メール登録PR #154に依存しない。Google権限審査不要の条件を採用し、外部管理画面の設定と実Googleでの公開確認は別途必要。


### タスク名: 1枚ずつのグループ切替と起動時の表示準備（関連 #11）
- 目的: スワイプの取りこぼしと、ホーム表示後に繰り返し待つ不快感を減らす。
- 主な実装内容: 選択を指を離した時に1枚だけ確定、隣接フィードの上限付き先読み、初回データ取得までウォンバットを表示。
- 完了条件: 連続/長い/逆方向/キャンセルと長押し、先読みと共有権限、初回失敗/再試行/再訪を検証しmake check・全E2Eを通す。
- 備考: 2026-09-15の追加依頼。前回PR #155では不十分との報告を受け、週次#149への追加対応。API/DB変更なし。

### タスク名: 起動時のグループ詳細取得数を制限
- 目的: 所属グループ増加による全員分の集計待ちをなくす。
- 主な実装内容: ホームの全グループ概要取得を廃止し、選択中と左右の隣の詳細キャッシュをカードにも共用する。
- 完了条件: 1/5/100グループで初回の詳細取得が最大3件、一覧以外の全件取得なし。切替・所属変更・非表示時停止を維持。
- 備考: #11、ユーザー追加依頼。表示用の名前・並び順一覧は取得する。読み込み表示/グラフとは別PR。


### タスク名: 読み込みをスピナーへ統一し自動更新表示を省く（#11）
- 目的: 読み込み状態の文章が繰り返し目に入る煩わしさを減らす。
- 主な実装内容: 必要な初回待機をスピナーに統一し、取得済みホーム/履歴/集計等の自動更新ラベルを削除する。
- 完了条件: 読み上げ名・動き低減・初回/再取得/失敗/再試行と入力保持を検証し、独立PRにする。
- 備考: 2026-09-15のユーザー追加指定。グラフとグループ取得上限の修正は別PR。

### タスク名: 個人・グループの履歴とグラフを統一
- 目的: 採用された操作プレビューを実データへ統合し、期間と指標を迷わず切り替える。
- 主な実装内容: 総負荷カレンダー、日週月グラフと直下の記録、期間指定/スワイプ、グループの日数/人数/メンバー別集計。
- 完了条件: 共有権限・ページング・編集/コピーを維持し、単体/DB/E2E・画像を含むPRを提出する。
- 備考: 週次#149の#112/#113。2026-09-15に最新プレビュー採用。旧目的別案は採用しない。#161の棒・タッチ修正を含む。

### タスク名: スタンプの送信と記録中の受信（#12）
- 目的: 仲間の保存済み共有記録へ反応し、記録中にも届いたことが分かるようにする。
- 主な実装内容: B案の選択パネル、グループ内スタンプと取消、受信一覧/未読、記録中の5秒表示、終了後の確認、権限・再送保護、migration。
- 完了条件: 2ユーザーの送受信・取消・既読、入力保持・連続受信・共有範囲・退会/削除をテストし、make checkとE2E、画面付きPRを完成する。
- 備考: ユーザーがB案と推奨①を採用。週次#149への追加理由は採用依頼。#12の担当IXYZONEは変更しない。最新main（#162統合済み）から独立ブランチで実装。

### タスク名: #164 B案の複数セット表示
- 目的: 現行の数値入力を維持して、今回のセットを見渡せるようにする。
- 主な実装内容: 今回を主に前回を添える一覧、メモの表示切替、終了操作を統合した上部、種目情報の補助表示。
- 完了条件: 320/390/430pxの複数行、入力/保存/編集/再送/メモ/スタンプの回帰を検証し、画像付きPRを提出。
- 備考: ユーザーがB案を採用。Closes #164、週次#149。#165〜#167と数値入力方式の変更は含めない。


### タスク名: アプリ表示名をE-GOTOREへ統一
- 目的: ユーザー指定の新名称を利用画面・インストール情報・現行手順へ反映する。
- 主な実装内容: ログイン/ホームのロゴ、metadata/manifest、API表示名と設定生成、READMEと現行運用資料の更新。
- 完了条件: 旧名称が現行の画面に残らず、既存データを維持し、画像付きの独立PRで検証する。
- 備考: 2026-09-18の追加依頼。#164とは別PR。公開URL・DB・保存キー・過去資料のファイル名は維持する。

### タスク名: カード・詳細の直接リアクションと画面を止めない送信（#179・#171）
- 目的: 採用A案の小さい角丸スタンプを記録のそばに表示し、送信待ちで閲覧を止めない。
- 主な実装内容: 定型6種類・旧反応保持、一覧/詳細の共有状態、上限付き一括集計、ユーザー別の送信待ちと再送、失敗復旧、migration。
- 完了条件: 権限・重複/取消・再訪・応答喪失・画面外失敗・小画面を検証し、画像付きPRのCI成功を確認する。
- 備考: ユーザーが最終プレビューを採用し本実装を指示。#180のオリジナル登録と#172の終了非同期化は今回対象外。

### タスク名: トレーニング中の受信スタンプをインライン表示へ統一
- 目的: 届いた反応を記録中に見渡し、…から送信者を確認できるようにする。
- 主な実装内容: 小さな絵文字＋種類別件数、固定位置の詳細導線、横スクロール、未読/新着/取得失敗の保持。
- 完了条件: 小画面・多種類・50件を超える集計・既読・入力位置保持を検証し、画像付きPRへまとめる。
- 備考: #179/#12の追加ユーザー依頼。数値入力・セット一覧を維持し、終了ボタンの文言も統一。API/DB/migration変更なし。

### タスク名: 設定から送信する目安箱（#189）
- 目的: アプリ内で意見・不具合を送信し、運営管理者だけが確認できるようにする。
- 主な実装内容: 設定の送信フォーム、認証必須の作成専用API、非公開DBテーブル、重複/連投防止、Supabase管理画面での確認手順。
- 完了条件: 入力制約・失敗再試行・アクセス制御を単体/DB/ブラウザで検証し、migrationと運用手順を添えてレビュー可能にする。
- 備考: 2026-09-20のユーザー実装依頼で週次計画#149へ追加対象。保存/閲覧先は既存Supabase。独立worktreeで既存変更を保持する。

### タスク名: デザインスタンダードの検討と統一（#186）
- 目的: 画面追加ごとの見た目・配置・操作のばらつきを防ぐ。
- 主な実装内容: 現行デモ、統一基準、採用したメモB案と終了操作の実画面への反映、styles配下へのスタイル集約。
- 完了条件: 採用した部品の基準・実装・見本が整合し、入力保護・小画面・テーマを検証する。全画面への展開は別途段階的に進める。
- 備考: docs/186-design-standard / PR #191で継続。メモは枠と背景色なし、終了確認は見出しと2ボタンを採用・実装。既存の数値入力・メモ編集・保存保護を維持。他画面の共通化とレビュー手順整備は#186の残作業。

### タスク名: ホームから進めるデザインの仕上げ
- 目的: 採用済みのメモ・終了と一貫するホームを検討する。
- 主な実装内容: ボタン・文字・余白を整理した独立見本と画像。
- 完了条件: ユーザーと採用案を確認し、他画面へ展開する。
- 備考: 完成までPRは作成せず、design/local-workshopでローカル検討。既存PRへのpushも行わない。

### タスク名: 記録体験を起点にしたデザインの仕上げ
- 目的: 説明文・待ち表示を減らし、トレーニング記録を止めずに進められる体験にする。
- 主な実装内容: 種目メモを選択画面へ移す見本、記録中のメモ入口、数値入力・セット・終了の情報階層、履歴・ホームへの展開。
- 完了条件: トレーニング画面、履歴、ホームの順に採用案を決め、共通する条件・部品を整理する。
- 備考: 2026-09-22のユーザー指定。画面の言葉は意味が明確な短いものに限り、「今回」など内部都合の曖昧な日本語を使わない。完成までPRは作成しない。
### タスク名: テスト実行時間の短縮（#192）
- 目的: 開発中の反復とCIの待ち時間を短縮し、必要な回帰検証を維持する。
- 主な実装内容: 対象E2Eの指定、ビルドを分けた反復チェック、CIの2 worker並列化と古いPR実行のキャンセル、ポーリング待ちの仮想時間化。
- 完了条件: 既存テストを削除せず全件が成功し、速度・実行コマンド・適用範囲を記録する。
- 備考: ユーザーの追加依頼によりmainから独立。PR #191のデザイン変更は含めない。

### タスク名: 記録画面変更時のE2E実行を適正化
- 目的: 記録画面の確認を保ちつつ、DB CIで画面E2E全件を毎回実行する待ち時間を減らす。
- 主な実装内容: migration検証は維持し、記録画面の小画面・前回比較・コピー表示を確認する専用E2EだけをCIから実行する。
- 完了条件: CIが対象E2Eを実行して成功し、全件E2Eは開発時の明示コマンドとして残る。
- 備考: 2026-09-22のユーザー指摘とPR #191のCI失敗を受けた対応。旧表示文言に依存する全件E2Eの更新は別途整理する。
