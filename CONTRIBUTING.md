# 開発への参加・進め方

初回の起動は [README](README.md)、仕様の入口は [docs/README.md](docs/README.md) を参照してください。
人とエージェントの共通ルールです。エージェントは [AGENTS.md](AGENTS.md) も読みます。

## 基本方針

- まず必要な機能を、利用者が一連の操作を試せる最小単位で完成させます。実際に使って得た改善点を次のIssueにします。
- 最初から全機能・汎用基盤を作り込みません。ただし、データを壊さないこと、共有範囲、入力検証、必要なテストは初版から扱います。
- 週の初めに取り組むIssueを選び、週の終わりに動作を見せて振り返ります。未完了は理由と残作業を記録して再計画します。
- Android／iOS対応を見据え、業務ルールとAPIを画面から分離します。モバイルの実装方式は別途決定します。

## 記録の役割

| 場所 | 記録すること |
| --- | --- |
| `docs/` | 合意した仕様・設計、資料の適用範囲 |
| GitHub Issue | 現状と変更後の差分、完了条件、担当、議論 |
| 週次計画Issue | その週の目的、選んだIssue、レビュー担当、振り返り |
| `task.md` | 機能・ドメイン単位の大きなタスク |
| `progress.md` | 日時付きの変更履歴、意図、検証結果、未解決事項、次のアクション |
| Pull Request | 実装差分、関連Issue、検証結果、レビュー |

仕様変更はIssueで合意し、`docs/` を更新してから実装します。同じPRに仕様と実装を含めても構いません。
Issueだけで既存仕様を上書きしません。資料と実装の差は「未実装」か「仕様矛盾」かを切り分け、採用判断が必要な矛盾は確認します。

## 毎週の開発サイクル

1. `main` の最新状態を起動し、既存Issueと前週の未完了を確認します。
2. 不具合・改善・追加機能をIssueにします。現状は画面、再現手順、関連コード、確認したコミットなど、後から確認できる根拠を添えます。
3. 「週次計画」テンプレートで、その週の目的と取り組むIssueを選びます。各Issueに実装担当1人とレビュー担当を決めます。未選択Issueはバックログに残します。
4. 1週間で実装・検証・レビューできないIssueは、利用者にとって動作確認できる単位に分割します。依存関係と先に決めるAPIを明記します。
5. 開発中は詰まった点や範囲変更をIssueに記録します。並行作業で同じファイルやDBに触れる場合は、先に分担を調整します。
6. 週の終わりに受け入れ条件に沿って実演し、完了／持ち越しと理由を週次計画に記録します。改善点は次のIssueにします。

優先度は、まず中心体験を通せること、次に利用を妨げる不具合、使って分かった改善を基準にチームで決めます。
週の途中で緊急対応を入れる場合は、代わりに何を持ち越すかも記録します。初回の準備作業などIssueがまだない場合も、PRと`progress.md`に目的と経緯を残します。

## Issueを書く

GitHubの「New issue」から、機能追加・改善、不具合、週次計画のテンプレートを選びます。

- 現状: 現在の動作と確認方法。未実装ならその旨を記載。
- 目的・変更後: 誰が何をできるようになるか。
- 根拠: `docs/` のファイル名・ページ・節、関連Issue。
- 範囲: 今回行うこと、後続に回すこと、依存Issue。
- 完了条件: 第三者が操作・テストで判定できるチェック項目。
- 影響: Web／API／DB／将来のモバイル、既存データへの影響。

改善を後回しにするときは、残す制約と後続Issueを明示します。曖昧な「後で直す」だけにしません。

## ブランチとPR

`main` を統合先にし、Issueごとに短い作業ブランチを作ります。

```bash
git switch main
git pull --ff-only
git switch -c feat/123-workout-record
```

ブランチ名は `feat/<Issue番号>-<短い名前>`、`fix/...`、`docs/...`、`chore/...` を使います。
上の `123` は例です。未コミット変更がある場合は、他人の変更を破棄せず整理してから切り替えます。

1. `docs/` 全体の資料一覧、関連仕様、`task.md`、`progress.md`、既存コード・テストを読みます。
2. 新しい大きな機能・ドメインは、着手前に `task.md` へ記載します。
3. 可能な限り先に失敗するテストを書き、小さく実装して検証します。
4. `progress.md` に意図、結果、未解決事項を追記します。過去の履歴を書き換えません。
5. 1コミット1目的でコミットします。例: `feat: ワークアウト記録の保存を追加`。
6. `main` 宛てのPRを作り、レビュー担当に依頼します。完了させるIssueは `Closes #123`、一部だけ対応するIssueは `Refs #123` で紐付けます。
7. CI成功と、原則として実装者以外の1人の承認を確認してマージします。仕様と完了条件の両方をレビューします。

競合の解消時は両者の変更意図を確認し、解消後に該当する検証を再実行します。
`task.md` は担当するタスクだけを更新し、`progress.md` は追記を基本にして競合を減らします。

## 完了条件と検証

- Issueの受け入れ条件を満たしている。
- 変更した振る舞いのテストがあり、関連する既存テストが通る。
- APIやDBの変更では、入力制約・共有範囲・既存データへの影響を確認している。
- UIの変更では、対象の画面幅、読み込み・空・エラー状態を確認している。
- 仕様と実装が一致し、`progress.md` に変更意図と検証結果がある。
- 未実施の確認は理由と残作業を明示し、必要な検証が失敗した状態で完了扱いにしていない。

通常のアプリ変更は、ルートで以下を実行します。

```bash
make check
```

これはbackendのlint・テストとfrontendのlint・単体テスト・buildを実行します。
DB統合テストは `TEST_DATABASE_URL` に専用DBを設定して実行します。記録・共有フローの変更では `make test-e2e` も実行します。準備方法はREADMEを参照してください。
文書・テンプレートだけの変更では、リンク・記載したコマンド・テンプレート構造・`git diff --check`を確認し、不要なアプリテストは追加しません。
テストを先に書けない場合は理由を `progress.md` に記録します。

DB変更時はローカルのSupabaseを起動し、migrationとseedの適用、schemaのlintを検証します。
`make db-reset` はローカルDBのデータを削除して再作成するため、失ってよい開発データで実施してください。
共有環境・本番への反映は別の作業として、対象と担当を確認して行います。

## 依存関係・環境変数

- Pythonは `backend/.python-version`、Bunは `frontend/package.json` の `packageManager` に合わせます。
- 通常のセットアップはlockfileを変更しません。依存を追加・更新した場合は、定義ファイルとlockfileを同じPRに含めます。
- Pythonの依存操作は `backend/` で `uv add <package>`、frontendは `frontend/` で `bun add <package>` を使います。
- 環境変数を追加したら、値を伏せた `.env.example` と起動手順を更新します。実際の秘密情報をコミットしません。
- `NEXT_PUBLIC_` の値はブラウザへ公開されます。サーバーの秘密情報を入れません。

## GitHub管理者による初回設定

以下はリポジトリ内のファイルだけでは有効にならない設定です。GitHub側で設定状況を確認してください。

- IssuesとPull Requestsを利用可能にする。
- `main` のruleset／ブランチ保護でPR経由の変更、1人以上のレビュー、CIの `backend`・`frontend`・`database` の成功を必須にする。チェック名は初回CIの実際の表示に合わせる。
- `main` のforce pushと削除を制限する。

週次計画はIssueだけで開始できます。Projects・ラベル・Milestoneは必要になった時点で追加します。
本書の追加だけでは、GitHub上のIssue発行・設定変更・デプロイは実行されません。
