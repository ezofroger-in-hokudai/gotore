# トレーニング中の画面設計

トレーニング開始後の種目選択、記録入力、仲間の記録カード、スタンプ詳細について、ユーザーとの検討で確定した判断をまとめる。2026-09-25時点で、種目選択と仲間カードは静的プレビューで合意中、記録入力は実装済みの採用形である。この文書を、次のプレビュー作成と実装の参照元にする。

共通の色・余白・操作領域・文字量の基準は [デザインルール](../design-system.md) に従う。ここに書く個別画面の判断は、その基準をこの導線へ具体化したものとして優先する。

## この導線で守る共通ルール

- 目的は「開始する → 種目を選ぶ → セットを記録する → その日のトレーニングを終える」を迷わず進めること。説明を足して理解させず、配置・短い文言・図で分かるようにする。
- 数値、種目名、保存済みセット、次に行う操作を主役にする。同期済み、共有先、保存しましたなど、通常時に内容を押し下げたり画面を揺らしたりする状態文は置かない。
- 白・薄いグレーと細い区切り線を基調に、赤は選択中・保存などの主操作・BESTだけに使う。大きい角丸カード、強い影、枠で囲んだ説明文、冗長な見出しは増やさない。
- 一覧もセットも横に詰め込まない。今日のメニュー、他人の記録、今回・前回のセットは、読み進められる縦の行で揃える。
- 読み込みによって既に操作できる場所を止めない。固定サイズのプレースホルダーだけを使い、取得完了で行の位置や画面の高さを大きく変えない。
- 種目選択の変更では、下記の記録入力・終了確認・終了後画面を旧UIへ戻したり、配置や文言を独断で変えたりしない。

## 記録入力（確定・変更しない範囲）

### 全体の配置と操作

- 一つの種目の記録を主画面にし、今回と前回のセットは同じ列の中で対になるように表示する。前回の記録は常に全セットを参照できるようにし、今回だけが離れたり、前回の有無で今回の操作位置が大きくずれたりしない。
- 保存済みセットは行全体を押して同じ場所で編集する。別画面・モーダルへ飛ばさない。編集中のセットをもう一度押すと編集を解除し、最新セットを押すと新しいセットの入力状態へ戻る。
- 新しいセットは入力フォームの`セットを追加`で保存・追加する。最後のセットの下に重複する`＋`入口は置かない。
- 次のセット入力は画面最下部に固定する。初期表示で上に浮かせず、セットを増やしても画面下からすぐ入力できるようにする。必要なときだけ入力フォームをしまえる。
- 保存後に自動スクロールしない。通常の保存成功トースト、`同期中`、件数表示なども出さない。未保存入力・通信失敗・競合だけは入力を保ったまま必要な表示をする。

### 数値とコピー

- 重量と回数は矢印だけの増減操作を使い、両方とも一回で `+5` / `-5` 相当を動かす。数値入力の見た目・大きさは保ち、矢印だけを少し押しやすくする。数値欄から上下の矢印までの間隔は同じにし、矢印の上下方向と増減方向を逆にしない。
- `重量 kg` と `回数 回` の見出し・単位は入力を開く操作にしない。数値を直接編集したいときだけ、中央の数値欄を押す。
- ルーラーの前後の数値を常時出さない。セット一覧の縦幅を圧迫するならルーラーを優先しない。
- 入力ドックは、数値・矢印の大きさを縮めずに、重量・回数の単位ラベルを`SET`見出しと同じ行かつ各入力欄の真上へ置いて上側の余白を詰める。推定1RMは二つの上矢印の中央に重ね、独立した行を作らない。下部は横幅を維持した`次の種目へ`と`セットを追加`だけを置き、セット一覧下の`＋`や`SET nを記録`の補助文は出さない。編集時も見出しは`SET n`だけとし、色は今回の入力欄だけで示す。
- 種目選択の行は左寄せとする。前回の`何日前`表示や右端の矢印の有無で、種目名の上下位置を動かさない。種目名だけを行の縦中央に固定し、前回表示はその直下に置く。
- 入力中の `前回をコピー` は、前回の対応セットの重量・回数をその場で入力欄へ入れる操作である。コピーしただけで保存・次セット追加・数値編集画面への遷移をしない。
- 対応する前回セットがない場合も、コピー操作で入力状態を失わない。次のセットを追加する必要がある場合は、まず今回側にその新しいセットを作り、その入力欄へ値を入れる。
- 推定1RMは保存状態に関係なく、重量・回数から毎回計算してセットの値の横に表示する。保存完了を待って出すものではない。

### メモと削除

- `今日のメモ` は入力フォームの直前に、`今日のメモ：［本文］`の一行で置く。今日この種目で残したい短いメモという位置づけにし、枠やカードで強調しない。タップでそのまま入力し、Enter または入力欄から離れた時点で保存する。保存・閉じるのボタンは置かず、編集時も文字を大きくしない。長文は一行のまま横スクロールし、フォームを下へ押し出さない。
- 種目メモも`種目メモ：［本文］`の一行で、今日のメモと同じ保存操作・文字サイズにする。本文の背景・囲み枠・大きい角丸は使わない。通信失敗・競合・端末下書きの保護は維持する。
- 削除は白黒のゴミ箱ピクトグラムを使い、ボタンのような大きい面にしない。今回セットの右側で位置を固定し、前回記録の文字数・有無で動かない。

### 終了

- トレーニング開始後の画面は、黒地の左に `E-GOTORE`、右端に `トレーニング終了` を置く共通ヘッダーを残す。`種目を選択` など画面名は表示しない。入力中の種目名は黒ヘッダーの直下に置く。横断的な基準は[デザインシステム](../design-system.md#共通ヘッダー)に従う。
- 共通ヘッダーは端末の上端から始まり、高さを固定する。種目選択・記録入力の切替や画面スクロールで上余白、位置、高さを変えない。
- 画面上の終了入口は `トレーニング終了`、終了確認の確定は赤い `終了する` とする。見出しとボタンの組み合わせで、その日全体を終える操作と分かるようにする。
- 種目選択と記録入力の終了入口は、同じ右端・同じ高さ・同じ赤い一行ボタンにする。角丸は両方とも8pxとし、片方だけを角丸なし、薄い文字、別の文字サイズにしない。
- 終了確認には `トレーニングに戻る` と、赤い一行の `終了する` を置く。余計な説明・確認文は置かない。

## 画面の順番

1. 黒い共通ヘッダーの左に `E-GOTORE`、右側に記録入力画面と同じ赤い `トレーニング終了` を置く。画面名は置かない。
2. 仲間は所属グループを横に切り替えられ、初期値は全所属グループの `すべて`。今日トレーニングした人（記録中・終了済みの両方）をアイコンで横に並べる。グループを選ぶと、そのグループの記録だけに切り替える。
3. `今日のトレーニング` は仲間一覧の下に置き、初期状態で閉じる。`2種目　5セット　3,240kg` のように種目数・セット数・総負荷を一行で表示する。`…` で展開し、全種目・全セットを縦に表示する。セットを横一列には置かない。
4. 部位は8分類を横一列に置く。各項目に控えめな輪郭と間隔を付けて、押して絞り込めることを示す。選択は薄い赤の面と赤い文字で示し、下線は使わない。複数選択は和集合で絞り込む。チェックボックス・選択説明・検索欄は置かない。
5. 候補は主部位が一致する種目だけを表示し、よく使う種目を先頭に並べる。各行の補助情報は前回実施からの日数だけとし、10日以上は `10日以上前` と表示する。
6. 最後の入口は `＋ 種目を追加`。部位が1つだけ選ばれているときだけ追加フォームの主部位に初期値を入れる。複数または未選択なら未選択で開き、追加前に利用者が主部位を選ぶ。記録中の追加シートはシート見出しの `種目を追加` だけを表示し、フォーム内には同じ見出しを繰り返さない。

`今日のトレーニング` の総負荷は、画面がすでに持つ今日のセットからその場で計算する。通信待ちにしない。

## 種目一覧（作り直しまでの方針）

- 種目の追加・分類変更・削除を行う一覧は設定からだけ開く画面として、改めてプレビューから設計する。記録中の種目選択には候補を選ぶ操作だけを置き、管理用の一覧・編集フォームを混ぜない。
- 現行の記録中シートを廃止するタイミングは、設定用一覧の採用プレビューと一緒に決める。候補選択、保存済み記録、種目名のデータ形式は維持する。

## 仲間アイコンと記録カード

- アイコンを押すと、その人の今日のトレーニング全体を下シートで開く。記録中でも終了後でも同じ入口にする。
- LIVEは既存の赤い外周と右下点を使う。最高重量または推定1RMを更新した人には、トレーニング詳細と同じ小さな `🔥` をアイコン右上に添える。LIVEとBESTは別の状態として同時に表示できる。
- 下シートでは氏名、種目数、セット数、総負荷を短く表示する。全セットを縦に並べ、すべてのセットに推定1RMを出す。
- BESTのセットだけ、重量・推定1RMを赤くし、横に小さな `🔥` を添える。BEST専用のカード、背景、説明文は追加しない。
- 記録カードのスタンプは `💪 🔥 👎 🤩 👏 👺` を常時並べる。種類ごとに1回送れ、同じスタンプをもう一度押すと取り消す。連打で同じ種類を増やさない。

## スタンプ詳細（`…`）

`…` は、誰がどのスタンプを送ったかを確認する下シートを開く。シート名は `リアクション` ではなく `スタンプ` とする。採用案はスタンプごとに送信者のアイコンをまとめるA案。

- シート上部の対象は、記録者名ではなく `9.25　3種目　9セット　5,460kg` のように日付・種目数・セット数・総負荷を一行で表示する。グループ切替は仲間アイコンの一覧にのみ置く。
- スタンプは記録単位で一つであり、同じ記録を見られるグループでは同じ反応を表示する。グループは人・記録を探しやすくするまとまりで、スタンプの送信先や絞り込みにはしない。閲覧者と記録者が所属しない、または記録が共有されていないグループには、記録・スタンプとも表示しない。最高記録を更新した日だけ、右端に小さな `🔥` を添える。
- `5件 3人` のような件数・人数の要約は表示しない。
- スタンプへ日本語の意味づけ（例: 「がんばれ」「えらい」）や人数テキストを付けない。絵文字と送信者アイコンだけで表す。
- スタンプが0件のときは、空の行や説明文を追加せず、スタンプの一覧を何も並べない。
- 送信者の表示はアイコンだけにする。名前を常時表示したり、アイコンを押して名前の一覧へ遷移させたりしない。

## 速度と読み込み

- 画面全体を止めるローディング表示や説明文は置かない。部位選択と種目の選択を最優先で操作できるようにする。
- 仲間アイコン、前回の日数、開いた後の記録カードだけは、同じ大きさを保つ控えめなプレースホルダーを許可する。取得完了時に画面の高さ・並びを変えない。
- 所属グループすべての当日記録は、グループごとの直列取得ではなく集約APIを1回呼ぶ前提にする。下シートの詳細・スタンプは開いてから個別に取得する。

### 実装済みの取得順

1. 開始直後は、すでに取得済みの種目候補を主部位でブラウザ内だけで絞り込む。仲間の通信を待たない。
2. `GET /groups/today-activity` で、閲覧者が所属する全グループの当日記録を一つのHTTP応答で取得する。グループが多いときの切替列は横スクロールする。
3. 仲間アイコンを押したときだけ対象記録の詳細を取得し、取得中は高さ固定のプレースホルダーを表示する。
4. スタンプの集計・詳細は記録カードを開いた後に取得する。種目候補や仲間一覧の初期取得へ含めない。
5. `＋ 種目を追加` は、部位が一つだけ選ばれている場合にその主部位を追加フォームへ初期値として渡す。設定用の種目一覧を経由せず、追加フォームを直接開き、追加後は種目選択へ戻る。

## 実装前に決めること

- 全所属グループをまたぐ「今日の記録」の集約API、重複記録の扱い。
- 前回記録がない種目の補助表示。

## 採用プレビューと実装照合

以下は実装時に必ず照合する固定プレビューである。途中比較の `-vN` ファイルは判断根拠ではなく履歴として残す。プレビューと本文が矛盾する場合は実装を止めて確認する。

| 対象画面・状態 | 採用プレビュー | 実装で照合する点 |
| --- | --- | --- |
| 種目選択・仲間アイコン・記録カード | [training-selection-adopted.html](../../frontend/public/previews/training-selection-adopted.html) | 終了ボタン、閉じた今日のトレーニング、全8部位、候補行、LIVE/BESTアイコン、全セットRM、常設6スタンプ |
| スタンプ詳細A | [stamp-details-selected.html](../../frontend/public/previews/stamp-details-selected.html) | `スタンプ` 見出し、日付・種目数・セット数・総負荷・グループ、BEST炎、絵文字と送信者アイコンだけの行 |
| 記録入力・メモ・終了確認 | [header-selected.html](../styles/header-selected.html) | 枠なしメモ、インライン編集、固定入力、セット操作、`トレーニングに戻る` と赤い `終了する` |

実装前・PR前に各プレビューを390px幅で並べて確認し、320px・430pxでは情報が欠けず、横スクロールや操作の重なりがないことも確認する。実装上の差分が必要なら、先にこの資料と採用プレビューを更新してユーザー確認を取る。
