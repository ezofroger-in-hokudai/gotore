# 画面文言（#48）

2026-09-08のレビュー指示を適用する。通常画面は短い項目名とボタンを中心にし、重複する説明や装飾的な見出しを省く。「自分用メモ」は「メモ」、「トレーニング日」は「日付」とする。

共有範囲、下書き、入力補助、メモ等の説明は初回ガイドへまとめ、設定から再表示できる。入力条件はHTMLの制約と簡潔なエラーで示す。削除・退出・コピー等の確認は対象と動作を短く示す。失敗時は入力保持と再試行の動作を維持し、必要な次の操作だけを案内する。

既存の機能仕様にある画面上の説明文・長いラベルは本方針を優先する。権限・共有範囲・保存・競合の業務ルールは変更しない。種目のグループ固有化は今回のマージ完了後に相談し、現在の本人用リストを維持する。

## 追加整理（#50）

ログイン・通信・入力・競合エラーも、状態と次の操作を短く示す。通知で対象名を繰り返さず、同じ成功通知を二重表示しない。初回ガイドは共有範囲・入力補助・履歴保持の意味を残して短縮する。読み上げ用の識別ラベル、入力制約、失敗時の復帰処理は維持する。開発用READMEの詳しい手順は今回の対象に含めない。

見た目と操作領域は[デザインルール](design-system.md)に従う。文字数を減らすために文字サイズやタップ領域を縮めない。

## v2の記録画面

メモは通常の本文と同程度の表示とし、空の入力導線は「メモ」。種目・前回・今回という説明ラベルは表示せず、種目メモの下に前回の本文がある場合だけ置く。タッチして編集し、読み上げ用の識別名は維持する。画面の「推定1RM」は「1RM」、「このセットを保存」は「次のセットへ」、「トレーニングを終了」は「トレーニング終了」とする。


## 主要画面の棚卸し（#114）

ユーザーの画面整理継続指定に基づき、次の範囲を整理する。文字を小さくして残す方法は採らず、必要な説明だけを操作時に確認できるようにする。

| 画面 | 対象 | 扱い |
| --- | --- | --- |
| 共通ヘッダー | 「日々の積み重ねを、仲間と。」 | 装飾文のため削除。ロゴと主操作を保持 |
| ホーム | グループ作成/参加を促す説明 | グループ未所属の状態だけを短く示す。作成/参加手順は既存ガイド |
| ホーム/グループ | 「最初のセットを残しましょう」 | 空状態に続く呼びかけを削除 |
| ホーム/記録 | 開始時の共有範囲・本人だけのメモ | 操作前の判断に必要なため保持。#107の移設でも残す |
| 記録 | 未送信・同期中・競合・再送・終了待ち | データ状態と次操作に必要なため保持 |
| 記録 | メモの種目/前回/今回という重複ラベル | 既に本文中心へ整理済み。読み上げ名を維持 |
| グループ | LIVE/TODAY・人数・退出/除外 | 状態・権限・影響が分かる表示を保持 |
| 履歴 | セット数・時間・空状態・読み込み失敗 | 記録と未取得を区別するため保持。直近優先の構成は#112で対応 |
| カレンダー | 日別総負荷kgと色の凡例 | 月内の相対表示・単位を示す。スコア停止の現行仕様はactivity-heatmap.md |
| 分析グラフ | 総負荷・推定RMの算式 | 「指標について」を開いたときだけ表示。開閉では通信しない |
| 分析ランキング | 現在共有されている記録を集計する説明 | 「共有済みの記録のみで集計」に短縮し常時表示 |
| 分析 | 比較不能・前期間・種目の選択条件 | 数字の意味が変わる条件のため保持。指標選択の構成は#113で対応 |
| 設定 | 「通知：準備中」 | 操作できない未提供項目のため削除。通知機能の実装は追加しない |
| 設定 | 設定保存の状態・振動非対応の説明 | 設定の反映可否に必要なため保持 |
| Sheet/確認 | 削除・退出・コピー先の確認 | 対象と結果・公開範囲を確認する文言を保持 |

API・DB・公開範囲・業務ルールは変更しない。指標説明は通常の文字サイズで表示し、開閉の操作領域を44px以上確保する。

## 選択済みの画面・項目との重複（2026-09-13追加依頼）

| 画面 | 整理する重複 | 変更後 |
| --- | --- | --- |
| 履歴カレンダー | カレンダーの上の「総負荷カレンダー」 | 月選択・総負荷kg・凡例を表示。セクションの読み上げ名は維持 |
| 月選択・日付マス | 月入力に重ねた「月」や未記録日の「—」の二重表示 | 月ラベルは読み上げ用に残し、未記録の記号は1つにする |
| 種目選択 | 部位ボタン直下で同じ部位名を見出しとして再掲 | 選択済みボタンと種目数で示す |
| 個人/グループの分析 | 選択済み指標名の直下で同じ名前を再掲 | 数値・単位・対象日数を表示。要約領域の読み上げ名で指標を識別 |
| グループランキング | ランキングタブ直下の「グループランキング」 | 選択中の指標・対象期間・共有済み記録の集計条件を表示 |

ホーム/履歴/設定などの画面タイトルは現在地の識別、設定のアカウント/アプリ/サポートは異なる項目の区切りとして保持。記録の前回/今回、日付、記録の投稿者・共有範囲、未送信/競合/取得失敗、削除・退出の確認は意味が異なるため削除しない。折りたたんだ記録での種目要約と、展開したセットの種目名も操作状態が異なるため保持する。

部位ヒートマップの上部フィルターは別途の画面指定として小さい文字と余白で「すべて＋8分類」を横一列に固定表示し、横スクロールをなくす。詳細は#136の修正版を参照。

## 2026-09-13の再棚卸し採用（#140）

[削減案](ui-copy-review-2026-09-13.md)に対するユーザーの了承を受け、A01〜A15・B01〜B09を今回の実装対象とする。本節は上記の凡例保持と常時展開した個人詳細の種目要約保持を更新する。

- カレンダーの凡例・目盛り・常設説明を削除し、説明を使い方へ移す。要約ラベルは「総負荷」、日付選択後の「主部位の内訳」は表示しない。
- 単一日で絞った一覧の重複日付と、展開した個人詳細の種目要約を省く。詳細の折りたたみ操作は保持し、閉じたときに必要な種目名は読めるようにする。旧記録の時間なし表示は省略。
- 記録の「タップで編集」、主ボタン内のセット番号補足、正常な終了確認の重複説明を省く。
- 種目一覧シート内の「種目リスト」、部位編集の説明見出し、グループの最新記録タブ内の重複見出し、招待の説明と再取得の二重案内を省く。
- 終了結果はねぎらい・保存済み・総負荷・種目数・セット数とホーム/履歴の操作を中心にする。英語の完了見出しと飾り文句は削除。
- 入れ子の前回トレーニング見出しを省き、保存ボタンを「保存」、分析の記録リンクを「記録を見る」に短縮する。

B10〜B12・C01〜C10は候補として資料に残し、今回の一括変更には含めない。共有先、前回/今回、未送信・競合・再試行・削除確認、読み上げ名を保持する。

残るB10〜B12・C01〜C10の採用比較は[後続Issue #141](https://github.com/ezofroger-in-hokudai/gotore/issues/141)で追跡する。

## 合意済み項目の取り残し（#146）

2026-09-14の継続依頼では、前回の振り返りが空の場合の「最初のトレーニングを記録してみましょう。」を「記録はありません」へ短縮する（#114の空状態の呼びかけ削減）。グループ詳細の初回取得中だけ残る「みんなの最新記録」見出しも省き、取得対象と読み込み表示で示す（A13）。通常ホームの区切り見出しは維持する。

採用済みA01〜A15・B01〜B09の他の文言は反映済み。B10〜B12や未選択のC案の一括削除は行わない。読み込み中の短い状態文は[共通キャラクター](design-system.md#読み込みキャラクター146)に置き換え、取得済み内容とエラー/再試行を区別する。


## 2026-09-15: 待機はスピナー、正常な自動更新は表示しない

ユーザー指定により、必要な初回取得・再試行中は短い文章の代わりに小さな回転リングを表示する。読み上げ用の取得対象名は保持し、動き低減時は静止する。起動のウォンバットは維持し、その下の「読み込み中…」は削除する。

表示済みのホーム・履歴・カレンダー・グラフ・前回記録を再取得する際は「更新中…」もスピナーも出さず、既存の数値・日付・内容を保持する。初回未取得と記録なしは区別する。失敗と再試行、共有権限の喪失、未送信・同期中・保存競合等の状態は隠さない。
