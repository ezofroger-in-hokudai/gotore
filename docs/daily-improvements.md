# 日常操作の改善 — 2026-09-07合意

初版に追加する範囲。#4・#5・#7のうち以下を会話で承認済み。未決の機能をまとめて採用しない。

## 記録入力（#5）

- 重量欄でEnterを押すと同じセットの回数欄へ移動する。
- 回数欄でEnterを押すと次のセットの重量欄へ移動する。最後のセットなら追加し、重量・回数とも空にする。重量は直前値を候補表示し、空欄のままEnterを押したときだけ採用する。直接入力した値は優先し、保存時に空欄を補完しない（ユーザーの追加指定）。
- 不正な数値では移動・追加しない。IME変換中・キーの長押しには反応しない。Enterで記録を確定送信しない。
- 最大30セット・20種目、数値制約、下書き、共有先の明示は維持する。追加ボタンも残し、タッチだけで操作できる。
- 入力密度は上げるが、セット数にかかわらず全項目を一画面に押し込まない。横スクロールなしで操作する。

## 表示名設定（#4の一部）

- 設定画面で本人の表示名のみ変更できる。前後空白を除き1〜20文字。
- Supabase Authのuser_metadata.display_nameを正とする既存ルールを維持する。成功後にAPIのプロフィール同期を行い、メンバー一覧と過去の共有記録にも新しい表示名を使う。
- Auth更新とDB同期は別通信であり、同期失敗時にはその状態を明示して再試行できる。メール・パスワード・他人の情報は編集しない。
- 同期はPOST /api/me/profileで行い、サーバーがAuthで再検証した本人情報だけを保存する。リクエスト本文のIDや表示名は使わない。表示名の更新方法は [Supabase updateUser](https://supabase.com/docs/reference/javascript/auth-updateuser) に従う（2026-09-07確認）。

### アクセス時の名前保持（#29、2026-09-07追加合意）

- Authに有効な表示名があれば優先する。Auth未設定・空白・文字列以外ならDBの既存名を保持し、プロフィールがない初回のみ「トレーニー」で作成する。明示的な「トレーニー」への変更は妨げない。
- GET /api/meも他のAPIと同じ同期・保持ルールを通し、確定した本人ID・表示名だけを返す。DB未設定・障害時は503とする。
- 設定画面を開くたびにGET /api/meで取得する。古いセッションの名前を編集欄に使わず、読み込み中・失敗時は保存操作を提供しない。取得後の編集中はポーリングで上書きしない。
- 通常の名称変更・過去共有記録への反映・認証と共有権限は維持する。既に消失した名前は自動復元せず、本人が設定画面で再保存する。DB migration・環境変数の追加は不要。

## グループ名変更（#7の一部）

- オーナーがグループ名だけ変更できる。前後空白を除き1〜40文字。所属しているだけのメンバーには変更権限を与えない。
- PATCH /api/groups/{id} にnameのみ送信する。オーナー以外・存在しないグループは404、入力不正は422。条件付きUPDATEで権限確認と変更を同時に行う。
- グループID・招待コード・メンバー・過去記録は変えない。同じ名前の再送は安全。並行した名称変更は最後にDBへ適用された値を使う。
- メンバー削除・退出・オーナー変更は後続の仕様合意が必要。
- ホーム・グループ画面の所属一覧も5秒ごとに更新し、他端末の名称変更を反映する。非表示中は停止する。メンバー詳細はグループ画面でのみ取得する。
- 同じ一覧の再取得中は表示を保持して共有先を消さない。別のグループ・ページの取得には以前のデータを流用せず、失敗時はエラーを明示する。ユーザー切替ではWorkspaceごと破棄する。

## 残るIssueの着手条件

名称設定は#4・#7の一部であり、親Issueを完了扱いにしない。以下は決定や環境が揃ってから次の実装対象を選ぶ。

| Issue | 次に必要なこと |
| --- | --- |
| #4・#25 | メール・パスワード変更の本人確認、SMTP・送信先・運用権限の合意 |
| #6・#7・#19 | 記録編集／削除・メンバー除外／退出後の過去共有記録と復旧・オーナーの扱い |
| #8 | 画面・文言案のレビュー。今回は名称設定と入力に必要なUIだけ変更 |
| #9・#10 | モバイル方式の比較調査と対象OS・ストア配布・チーム条件の確認。移植基盤は未採用 |
| #11 | 本番相当の測定条件・件数・目標値の合意と計測。今回の一覧保持だけで性能目標達成とはしない |
| #12 | スタンプ種類・自分への送信・取消・既存記録編集との整合性 |
| #13・#14・#15 | #14の初回は[セット数ヒートマップ・日別記録](activity-heatmap.md)を採用（子#35）。SCORE・BEST・種目別推移・ランキング等の定義は後続 |
| #16・#21・#22・#26 | 採用範囲、共有／外部送信の同意・プライバシー・費用・運用条件 |
| #17 | 初版に採る入力補助・前回値・BEST・目標の仕様 |
| #20 | 再発行だけか有効期限／無効化も行うか、旧コード利用・再参加の運用合意 |

| #17・#18 | 初版に採る入力補助と、ガイドの表示済み状態・再表示・スキップの仕様 |
| #20 | 2026-09-08の追加指示により、[招待コード再発行](invite-code.md)の範囲で実装する。期限・招待停止は導入しない |
| #23 | iPhone／Android実機と公開環境へのアクセス、端末での操作確認 |

#18は2026-09-08のユーザー追加指示により、[初回利用ガイド](onboarding.md)の範囲で実装する。

名称設定・入力改善はPR #27でmainへ統合済みで、同PRのCIは実Supabase共有E2Eを含め成功した。#29の名前保持修正はローカルで実装・検証済みだが、Docker停止により今回の実Supabase共有E2Eは未完了。push・PR・本番反映は別途行う。最新の結果はPRとprogress.mdで追跡する。
