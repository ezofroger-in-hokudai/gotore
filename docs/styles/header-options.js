document.getElementById("width").addEventListener("change", e => document.documentElement.style.setProperty("--example-width", `${e.target.value}px`));
document.getElementById("long").addEventListener("change", e => document.querySelectorAll(".exercise-information").forEach(el => { el.textContent = e.target.checked ? "インクラインダンベルベンチプレス" : "ベンチプレス"; }));
document.querySelectorAll(".memo-toggle").forEach(button => button.addEventListener("click", () => { const memo = document.getElementById(button.getAttribute("aria-controls")); memo.hidden = !memo.hidden; button.setAttribute("aria-expanded", String(!memo.hidden)); button.setAttribute("aria-pressed", String(!memo.hidden)); button.textContent = button.closest(".current") ? (memo.hidden ? "▤ メモ" : "✓ メモ") : (memo.hidden ? "メモ ▾" : "メモ ▴"); }));
document.querySelectorAll(".finish-training,.memo-text,.exercise-information").forEach(button => button.addEventListener("click", () => { document.getElementById("status").textContent = button.matches(".finish-training") ? "実アプリでは、ここから終了確認を開きます。この見本では終了しません。" : "今回は配置の見本です。本文の編集・種目情報は現行の操作を維持する想定です。"; }));
