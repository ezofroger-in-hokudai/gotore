# ボタン・メモの具体案（#186 / PR #191）

2026-09-21。ユーザー指定により、**メモとトレーニング中の画面は現行の見た目・配置・操作を基準にする**。正本は [既存基準](../design-system.md)。[現行メモの見本](index.html#memo)を参照する。

## 合意した出発点

メモの色・枠・開閉・インライン編集、トレーニング中の数値入力・セット追加・終了操作を維持して、今の実装から共通ルールを整理する。先に提示したメモA/B比較とシート編集案は撤回。独立見本は現行画面の一部を抜粋したもので、記録画面全体の代替ではない。

以下の値と[ボタン見本](workshop.html)は、その他の画面への展開を検討する候補。記録中のボタンをこの候補へ一律に置き換える承認ではない。

## 役割と候補値

| 対象 | 候補 | 適用条件 |
| --- | --- | --- |
| 主操作 | 赤塗り・白文字 | 保存、次のセットなど、その操作領域で最も進めたい操作 |
| 補助操作 | 面色・可視の枠 | 戻る、キャンセル、別の進め方 |
| 文字操作 | 下線付きの文字 | 編集、閉じる。文章と混同しない |
| 削除 | 赤文字・赤枠 | 対象・結果の確認内でのみ強調。色だけで意味を伝えない |
| 操作領域 | 幅/高さとも最小48px | ラベルが長ければ折り返して高さを伸ばす |
| 角丸 | ボタン/入力10、メモ12px | 顔画像・スタンプは別役割。箱全体への一律適用はしない |
| 文字 | 本文/ボタン14、入力16、補助12px相当 | remで指定。補助サイズへ重要情報を追いやらない |
| 余白 | 4/8/12/16/24/32px | 部品内12〜16、同じ操作群8、説明節24〜32。記録画面全体は別途評価 |
| 面と枠 | 通常の区切り線と操作部品の枠を別トークン | 薄い線だけで入力欄を識別させない |

見本の`workshop.css`に候補トークンを集約。暗色の主ボタンは白文字の読みやすさのため現行より暗い赤 `#bd2529` を試す。デモ外枠の大きな見出し・節間・カードはアプリの候補値ではない。

## 状態と操作の契約案

| 状態 | 見た目・操作 | 避けること |
| --- | --- | --- |
| 通常 | 役割に応じた色・枠・ラベル | 同じ保存操作を画面ごとに別の形にする |
| 押下 | 明度を下げる。位置は動かさない | タップでレイアウトがずれる |
| フォーカス | 外側の輪郭で識別 | outlineを消す |
| 無効 | 薄くする、操作を受け付けない | 薄い色だけで送信中と区別させる |
| 保存中 | 「保存中…」、重複操作を抑制 | 成功する前に保存済みと表示 |
| 失敗 | フォーム内に理由と再試行方法、入力保持 | シートを閉じる、入力を消す |
| 空 | メモなしと示す。空文字で再編集 | 「メモはありません」を保存内容に混ぜる |
| 成功 | 成功応答後に本文更新・編集を閉じる | 通信結果と独立した見た目だけの成功 |

本番の本人限定・文字数・競合検知・端末への下書き保存は既存の[メモ仕様](../private-workout-memo.md)に従う。上の状態表は他画面のフォーム整理案であり、メモ編集の方式変更を意味しない。

## 次の進め方

1. 現行のメモ・記録画面から、色・余白・角丸・操作状態の共通ルールを抽出する。
2. ホーム・設定・管理に展開する部品を整理する。記録画面特有の密度や配置は用途別の基準として保持する。
3. 共通化時は見た目と操作を維持し、見本も同じ共通実装を参照する。
4. 適用後の代表画面を比較する。カードの共通メニューは#184との境界を保つ。

採用記録: 2026-09-21、ユーザー「メモとかトレーニング中のやつは今のもとにしましょう」。この合意を#186/PR #191と基準の正本に記録。その他の画面の候補値は未確定。

## 追加検討: メモ・終了の入口

ユーザーから「メモとトレーニング終了のやつなんか変だからそれよくしたい」「いったん例とか見せてください」と追加依頼。現行を起点に、入口の見た目と配置だけを[2案](header-options.html)で比較する。本文の別配色・シート編集を再提案するものではない。A/Bは未採用。

## 最新の配色合意

ユーザーが薄いグレーのメモ、グレー枠の終了入口、赤い終了確定という提案に同意。[更新見本](header-selected.html) / [画像](header-selected.png)。アイコンは短い文字と併記して具体化した。Aの横一列配置は仮置き。現行を基準にする従来の合意に対する部分的な更新で、メモの編集方式・数値入力・保存/終了の保護は維持する。アプリへの適用は未実施。
