# スタンプ（#12）

ユーザーが2026-09-15に採用したB案（選択パネル）と推奨①（記録中に5秒表示＋未読）を実装する。

- 種類は👏おつかれ！・🔥ナイス！・💪一緒にがんばろう・👀見たよ。本人には送れない。1記録・1グループ・1送信者・1種類につき1個。複数種類を併用でき、取消できる。送信成功前に件数を確定しない。
- 送信元グループの現在のメンバーだけが閲覧・送信できる。記録の共有と投稿者の現在の所属も検証する。同じ記録を別グループへ共有しても、反応・送信者は各グループ内に限定する。本人の受信一覧には自分宛てだけをグループ名付きでまとめる。
- 保存済みセットのある記録が対象。トレーニング中も送れる。編集後は保持。記録削除、共有解除、送信者/受信者の退会で対象反応を除去し、再参加で復活させない。
- 受信一覧は50件ずつ。表示したIDだけ既読にする。5秒表示を済ませた状態と、一覧で確認した既読を分けてサーバー保存する。同じ送信の再送で新着を増やさず、取消後の新しい送信は別の反応として扱う。
- 記録画面を開いている間は今回の記録の受信を定期確認。5秒だけ名前とスタンプを表示し、以後は未読件数を残す。入力値・フォーカス・入力欄の位置を保つ。複数件はまとめて表示。一覧はタップ時だけ開く。
- 終了後にも今回の反応を確認できる。アプリ非表示中は取得を停止し、復帰時に再確認する。Push通知、音、振動は今回含めない。配信即時性は保証しない。

本実装ではAPIの認可・DBの整合性を検証し、機能の公開前にmigrationを適用する。採用プレビューは別のローカル文書に保持。最新の実行結果はprogress.md/PRに記録する。

## APIと取得範囲

| API | 用途 |
| --- | --- |
| `GET /groups/{group_id}/workouts/{workout_id}/stamps?offset=0` | 対象の反応を50件ずつ、総数・人数・自分の種類・送信可否を取得 |
| `PUT /groups/{group_id}/workouts/{workout_id}/stamps/{kind}` | 同じ種類の送信を冪等に確定 |
| `DELETE /groups/{group_id}/workouts/{workout_id}/stamps/{kind}` | 本人が送った種類だけ取消 |
| `GET /stamps/inbox?group_id=...&workout_id=...&offset=0` | 本人宛ての受信一覧。group/workoutは任意 |
| `POST /stamps/seen` | 表示済み/既読をID単位で保存（最大100件） |

いずれも `/api` 配下、認証必須。共有記録の取得と送信は所属・投稿者・記録・共有行をロックして、共有解除と競合させない。外部入力は型・固定種類を検証しSQLへバインドする。新規テーブルはRLSを有効にし、anon/authenticatedの直接アクセスを拒否する。

通常のフィードではスタンプボタンだけを追加し、反応一覧はパネルを開いたときに取得する。開いているパネルと記録中の受信確認は10秒周期。非表示タブでは止め、復帰時に再取得する。グループ数に比例した常時取得は行わない。通知の表示済み保存が失敗した場合は再試行を出す。

## 公開時の手順

1. アプリ更新前に、対象Supabaseへ `supabase/migrations/20260915110000_workout_stamps.sql` を適用する。新規テーブル・インデックス・共有解除時の整合性トリガーの追加。既存記録のリセットは不要。
2. アプリ/APIを同じPRの版へ更新する。アプリ環境変数の追加はない。
3. 2人で保存済み共有記録へ送信し、受信・取消・未読を確認する。

今回ローカルで追加適用・DB lint・認可テスト・2ブラウザの送受信を確認した。本番DBへの適用は未実施。公開プレビューでも接続先にmigrationが必要。

## 画面

[選択パネル](images/stamps/picker.png) · [記録中の受信](images/stamps/arrival.png) · [受信一覧](images/stamps/inbox.png) · [終了後](images/stamps/result.png) · [高さ720pxの記録画面](images/stamps/compact-320.png)

架空のテストユーザーがローカルDBへ保存したデータを、実際のアプリで表示した画像。compact-320は既存の記録テスト用データで小画面の配置を確認したもの。

## 2026-09-18 採用A案（#179・#171）

ユーザー採用の直接リアクションへ変更する。カードと共有詳細に高さ30pxの角丸スタンプ・件数、＋、…を置く。48pxのタップ領域を維持。＋は💪がんばれ・🔥まだまだ・👎バッド・🤩すごい・👏えらい・👺天狗の6種類を絵文字だけで選び即送信する。古い4種類のID/意味/反応は保持し、既存反応の表示と取消を継続する。オリジナル登録は今回含めない。

本人・グループ・記録・種類をキーに送信状態を画面から分離し、一覧と詳細で共有。押した時点で仮の件数/選択を出し、送信待ちを示す。失敗時は確定値へ戻して再試行を出す。PUT/DELETEの成功と再取得失敗を区別する。閉じても処理が継続し、未完了は端末へ保存して再訪時に確認・再試行できる。OS終了中の実行は保証しない。権限喪失は送信を止め、未確定を成功扱いしない。

表示中の記録だけをグループ別・最大50件の一括集計で取得し、件数を送信者一覧の最初の50件から計算しない。非表示タブでは定期取得しない。種別拡張migrationをアプリ/APIより先に適用する。具体的な検証・制約はprogress.md/PRで追跡する。

実装はアプリ内の画面移動中の送信継続を対象とする。ブラウザ終了後の送信は保証しない。送信待ちはユーザー別・操作別のlocalStorageに保存し、再起動後は確認して再試行する。復元時は状態を照合し、既に反映済みならPUT/DELETEを繰り返さない。同じ種類は送信確定まで再操作を防ぐが、他の種類・画面は操作できる。再取得は10秒ごと、非表示タブでは停止。非表示の古い記録キャッシュは200件を目安に破棄し、表示中・送信待ちを優先する。

本番は `supabase/migrations/20260918150000_inline_stamp_kinds.sql` を先に適用してからアプリ/APIを更新する。既存データの変換・削除はない。旧種類も一覧と取消に残す。画面画像は `docs/images/inline-stamps/`。

## トレーニング中の受信表示（2026-09-18追加指定）

記録中の大きい受信ボタンを、カードと同じ小さな角丸の絵文字＋種類別件数に置き換える。件数は受信APIの全件集計 `counts` を使い、最初の50件から計算しない。複数グループから同じ種類が届いた場合は自分宛ての反応数を合算する（同じ人でもグループが異なれば別の反応）。自分への送信操作は追加しない。絵文字のタップと右端の…で、既存の受信詳細を開き、誰から・どのグループから届いたかを確認する。

新着は5秒間の枠の強調と読み上げで知らせ、絵文字と件数はその後も残す。送信者名の一時表示は詳細へ集約する。未読は…の小さな印と読み上げで示し、詳細で実際に表示した反応だけを既読にする。多種類は一行の横スクロールで、…は固定し、入力位置・セット一覧の高さを変えない。0件は「スタンプなし」、未取得はスピナー、失敗は同じ行の再試行操作とする。追加指定により、終了後の受信欄、ホーム/グループの受信入口も共通の絵文字＋件数・…表示に統一する。送信者一覧と選択対象の枠も角丸にする。記録画面の終了ボタンは省略せず「トレーニング終了」と表示する。
