# 更新失敗時の前回値の表示

2026-09-13、ローカルChromium・390×844px、架空ユーザー/記録と503応答で撮影。グループの前回のTODAY人数と記録を保持し、LIVEは未確認として「—」、更新失敗と再試行を表示する。

`frontend/tests/e2e/refresh-retention.spec.ts` の「ホームとグループ…」を `REFRESH_SCREENSHOTS=1` で実行すると再撮影できる。実ユーザー情報を含まない。通常の取得成功、503、再試行、403での消去を同じテストで検証する。
