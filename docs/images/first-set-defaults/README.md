# 前回最初のセットを入力初期値へ反映（#120）

Linux / Chromium、390×844px。`first-set-defaults.spec.ts` の架空ユーザー・固定API応答で撮影した。

前回の1セット目62.5kg・8回を初期表示する。前回2セット目75kg・10回を使わず、今回のセットはまだ保存していない。前回なしは20kg・10回、今回の記録・手入力・復元値を優先する。

[入力画面](record.png)。実機PWAや本番のデータ・速度を示す画像ではない。
