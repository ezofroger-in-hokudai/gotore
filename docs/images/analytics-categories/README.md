# 分析の分類表示

2026-09-13、ローカルChromium、幅320/390/430px・高さ720pxで確認。架空ユーザーと固定集計値による本人の履歴グラフで、継続カテゴリを選択した画面。実ユーザー情報・実AI応答は含まない。

`frontend/tests/e2e/analytics.spec.ts` の「量・強さ・継続から選び…」を `ANALYTICS_SCREENSHOTS=1` で実行すると再撮影できる。Web/APIは通常のE2E手順で起動する。

分類3個、種目・期間・選択指標、グラフを優先し、比較・表示設定・集計方法は下部から展開する。カテゴリの切替、重量/RM・成長ランキング、期間による利用可否、追加取得なし、横幅をE2Eで確認した。実機PWAでの検証は別途 #23。
